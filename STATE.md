# Synapse QA 全局状态记录

## 当前阶段

项目处于自动化测试平台从原型向可用系统推进阶段，已包含前端管理台、Go 后端、PostgreSQL 初始化和执行器模块。

## 已完成重点

- 首页工作台已进行多轮视觉和信息密度优化。
- 已实现登录、用户管理、角色管理、操作日志等系统管理能力。
- 测试配置已覆盖项目配置、项目产品、产品模块、测试对象等 CRUD。
- 界面自动化已包含页面元素、页面步骤、测试用例、全局变量等基础页面。
- 页面元素支持项目产品与模块联动。
- 页面步骤支持项目产品、模块、所属页面联动。
- 页面步骤工作台支持画布拖拽、节点拖动、画布平移和缩放。
- 已实现刷新后回到用户原浏览页面的路由状态恢复能力。
- 已创建 Loop Engineering 前端组件。

## 当前未提交关注点

- `frontend/src/pages/UIAutomationPage.js` 存在页面步骤工作台相关改动。
- `frontend/src/styles/global.css` 存在页面步骤工作台和 Loop Engineering 样式改动。
- `frontend/src/components/LoopEngineering.js` 为新增公共组件。
- `SKILL.md`、`STATE.md`、`tasks/current-task.md` 为项目规范与状态记录文件。

## 项目决策

- 所有正式开发任务必须在项目根目录 `F:\Synapse QA` 内完成。
- 不再为普通功能开发创建独立 git worktree 或独立模块。
- 功能实现必须接入当前前后端既有目录结构，避免形成无法运行、无法集成的孤立代码。

## 运行环境

- 本地前端常用地址：`http://127.0.0.1:4173/`。
- 后端 API 常用地址：`http://127.0.0.1:8080/`。
- PostgreSQL 本地密码曾配置为 `123456`。

## 后续方向

- 完善页面步骤节点配置的数据保存与后端持久化。
- 建立步骤调试接口和执行日志模型。
- 将 WEB/安卓操作方法映射到真实执行器能力。
- 增加 AI 自动生成测试用例、报告诊断等能力。
- 将 Loop Engineering 组件挂载到合适的项目规范或帮助页面。
