# Changelog


## Unreleased

- 新增性能测试模板操作手册：说明六类负载场景、环境与请求配置、阈值和自动停止、冒烟与正式执行、实时监控、报告状态、基线劣化、脱敏导出、五字段 Cron 定时规则，并提供推荐配置模板、安全建议和故障排查。
- 优化性能测试定时规则页面：修复两个直属面板触发主题首行整屏伸展的问题，将筛选、操作栏、状态提示、`TablePanel` 和分页收敛到与压测方案页相同的单一管理面板；移除独立 Hero/H1/统计区和第二个列表面板，统一字体、行高、圆角与阴影，空数据时仍显示统一分页器，并补齐左侧菜单图标。
- 完成性能测试 P2：支持复用产品测试环境进行 Base URL 解析和单次环境覆盖、通过执行设为基线、P95/P99/错误率/RPS 基线对比与同维度趋势、超过 10% 的劣化通知、脱敏 CSV/JSON 报告导出，以及带 IANA 时区、数据库租约和幂等保护的五字段 Cron 定时触发；新增定时规则管理与多环境执行界面。
- 完成性能测试 P1：支持 mixed 多接口按权重选流、`abortOnFail`、实时 SSE 监控与断线续传、RPS/延迟分位/错误率/VU 趋势图、状态码与错误 Top N、阈值状态，以及最多 3000 点的全程降采样报告；同时加入 500 VU 硬上限、单请求 smoke、4 MiB 终态回调限制和运行恢复保护。
- 完成性能测试 P0 真实 k6 闭环联调：安装 k6 v2.2.0，执行器上报 `supportedTypes=perf` 与 `checks.k6=true`；修复 PostgreSQL nullable `error_message` 扫描失败、run 停留 `pending`、后端下发 payload 与 runner 字段不一致、perf 回调仍使用通用 TaskView 导致 HTTP 400 四个运行时缺陷。真实 baseline run 已完成并回填请求数、P95、错误率、RPS、k6 版本与执行耗时。
- 修复性能测试触发后 run 永久停留 `pending`：创建/幂等命中后条件推进至 `queued`，调度器同时兜底推进遗留 `pending` 记录后再分发。
- 实施性能测试 P0 后端：性能测试模块从占位升级为真实执行闭环。数据模型升级（scenario_type/load_config/environment 替代 load_mode、平台结构化阈值、执行记录补齐快照/task_id/幂等键/回调凭据哈希/状态机字段等 20+ 列与三个唯一索引）；状态机固化（pending→queued→dispatching→dispatched→running→stopping + completed/threshold_failed/execution_failed/timed_out/canceled，条件更新 + 非法转移 409）；幂等触发（idempotency_key 唯一约束）；独立调度器（复用执行器选择原语 + k6 检测校验）；回调鉴权（task_id 与 Bearer Token 分离、终态失效、410/409）；执行器掉线/重启恢复循环（只查询只补偿取消、绝不自动重投）；expected_finish_at 动态计算；smoke 冒烟接口（/perf/smoke）；权限三档（perf.plan.read/manage/execute）；完成/失败通知。
- 实施性能测试 P0 前端：性能测试重构为独立路由页面（/performance/plans/:id/edit、/performance/runs/:id），方案编辑页四区块（场景卡片 + Params/Headers/Body 分 Tab + 负载配置与实时预览 + 结构化阈值表格）、执行确认面板（生产环境二次确认）、报告详情三屏（结论/阈值/诊断，区分五种终态失败类型）、冒烟测试请求、权限按钮显隐。
- 实施性能测试 P0 执行器侧：执行器新增 perf 任务类型与 k6 压测 runner，按场景类型（baseline/ramp/peak/stress/soak）渲染 k6 脚本并调用 k6 子进程执行，动态计算超时、NDJSON 本地采样兜底、阈值判定与取消部分结果回传；回调带 Bearer Token 并指数退避重试，心跳按 k6 可用性上报 perf 能力，任务提交与取消幂等（含取消墓碑）。
- 新增性能测试业务模块：新增「性能测试」一级菜单（压测方案、测试报告），压测方案支持 K6 风格负载配置（固定并发 vus+duration / 爬坡 stages）与阈值断言 thresholds 的 CRUD 及触发执行占位，测试报告承载 K6 指标（总请求数、平均耗时、P95、错误率、RPS）展示；底层压测引擎选定 Grafana k6，真实执行由执行器后续接入。
- 修复打包版执行器「调试页面步骤」报 `playwright install`：PyInstaller 打包后 Playwright 会把浏览器路径指向驱动目录下的 `.local-browsers` 导致找不到浏览器，现已自动指向用户级 `ms-playwright` 缓存。
- 修复执行器连接异常后无法自愈的问题：执行器心跳因 Token 鉴权失败（401）停止后，即便 Token 被修正也无法恢复；现在 `authenticate()` 连接成功后会写回新 Token 并重启心跳循环，无需手动重启执行器进程即可恢复在线。
- 修复左侧菜单重复的「AI 智能」入口：删除 `menuData` 中重复声明的「AI 智能」菜单组，仅保留一个入口，子菜单不变。
- 新增 AI 智能工作台：支持按用户配置多模型与使用偏好、连接测试、SSE 对话及受控测试资产工具调用；前端提供 AI 助手、智能断言、用例生成、失败分析和 AI 设置入口。
- 新增「DeepSeek 极简」浅色主题并设为默认：基于 DeepSeek 官网设计令牌（主蓝 #3964FE、hover 变亮、蓝灰文字、胶囊按钮、圆润圆角、柔和阴影、2px 品牌蓝焦点环），在外观设置中可随时切回原有五套主题。
- 优化 DeepSeek 主题应用骨架：侧边栏 250px、品牌区简洁圆角、64px 顶栏、蓝灰面包屑与页面头部、6% 蓝灰 hover 反馈，统一按钮/卡片/表格/输入框/导航/弹窗的视觉层级。
- 优化 DeepSeek 主题 AI 工作台：将能力卡片、模型管理、对话界面、消息气泡、工具消息与输入区从原紫色/绿色系对齐为品牌蓝与蓝灰视觉，保留第三方服务商标识色。
- 优化 DeepSeek 主题业务页面：数据工厂、执行记录与测试报告、接口调试器、页面步骤画布工作台逐步对齐品牌蓝与蓝灰视觉，统一圆角、柔和阴影与 6% hover 反馈，保留节点类型色、接口方法色与状态色等业务语义色。
- 修复画布节点文字模糊：提升节点类型、标题、标签与状态文字的字号与字重，并修复状态文字对比度不足，提升画布缩放后的可读性。
- 优化节点操作选择器：由居中模态框改回锚定输入框的下拉面板，保留分类树导航、操作列表、键盘导航与懒渲染。
- 修复执行器配置页：恢复执行器列表管理（在线状态、运行中/排队中负载、10 秒自动与手动刷新）、新增执行器及一对一专属 Token 生成，并补齐卡片样式；共享 Token 管理保持不变。

- 优化页面元素采集的列表操作兼容：对表格行内重复的按钮或链接，自动以当前行唯一业务文本作为锚点，生成“行上下文 + 行内操作”的组合 XPath；兼容原生、ARIA、Element UI、Ant Design 及 `yv-*` 等自定义表格行结构；如无唯一行锚点，仍保持低可信提示，避免错误保存。
- 修复列表行内操作元素（编辑/详情/删除）无法满足“唯一且评分 ≥40”门槛：行识别扩展至原生、ARIA、Element UI、Ant Design 及自研表格/列表（含 `li`、`role=listitem`）；锚点选择跳过含交互控件的操作列；支持 `span`/`div` 等非按钮操作元素；固定操作列缺少业务锚点时回退为当前被点击元素对应的全局同名操作索引 XPath（row-index 策略，评分 55），避免错误命中表头，仍可保存并提示回放验证。
- 新增页面步骤“美化画布”功能：按流程层级、分支宽度与画布空间自动排列节点，支持条件分支布局、未连接节点整理、主题化预览、应用/取消确认；应用后立即持久化完整画布布局。
- 修复页面步骤画布美化：自动布局按可用画布空间居中并加入节点碰撞修正，未连接节点不再遮挡主流程；SVG 连线和真/假分支标签在预览与正式画布中保持可见。
- 优化页面步骤画布美化：未连接节点改为自适应矩阵布局，单节点居中；应用或取消预览后恢复节点拖拽与坐标持久化。
- 优化页面步骤画布美化：已连接流程按稳定顺序自动换行排为多列矩阵，线性流程不再固定纵向单列。
- 优化页面步骤画布美化：已连接流程采用紧凑蛇形网格，跨行连线绕经节点外侧，提升流程可读性。
- 修复页面步骤画布美化：流程从画布左上安全边距锚定，不随画布比例居中偏移；增大节点行、列间距以避免上下覆盖。
- 修复页面步骤画布：从左侧面板新增的未保存步骤立即显示为可编辑、可拖拽、可连线的草稿节点。
- 修复页面步骤画布：新增未保存草稿节点默认不自动连线，作为自由节点等待用户手动连接。
- 优化页面步骤画布：节点连线端口改为始终可见的中文操作控件，并支持目标高亮和 Escape 取消连线。
- 优化页面步骤画布：连线改为点击起点“连线”后直接点击目标节点卡片完成；条件节点先选择真/假分支。
- 修复页面步骤画布：连线起点状态改为同步维护，避免节点与画布事件竞争导致无法完成连接。
- 修复页面步骤画布：连线模式增加画布顶部独立提示与取消操作，确保已选起点和下一步操作可见。
- 优化页面步骤画布：普通节点连线改为依次点击起点和目标两张卡片，条件节点仅需在起点选择真/假分支。
- 修复页面步骤画布：删除节点改为本地移除，仅点击「保存画布」后才真正持久化删除。
- 优化页面步骤“节点操作”选择器：改为居中模态框，左侧分类树、右侧操作列表，支持 hover 高亮、选中态区分、上下箭头/回车/Esc 键盘导航与懒渲染；回填文本保持“分组 / 操作名”格式，后端与导出配置不变。


- 优化 Element UI 下拉选项采集：点击选项内部文本时自动提取可操作的 `li.el-select-dropdown__item`，并生成限定当前可见下拉面板及选项文本的稳定 XPath；兼容框架 class 被敏感扫描误判导致候选丢弃的问题。
- 页面元素采集改为分级保存：唯一且评分 ≥70 的候选可直接保存；唯一且评分 40–69 的候选可保存并提示执行前回放验证；不唯一或评分低于 40 的候选仍会阻止保存。
- 页面元素采集新增 XPath 定位提取：基于 `data-testid`、`id`、`name`、`aria-label`、`placeholder` 及按钮文本等稳定语义生成 XPath，保证其参与真实匹配数校验并展示；唯一的稳定属性 XPath 与语义定位器不再因 DOM 深度跌破可靠性阈值。
- 优化页面元素提取的框架兼容性：支持 Element UI、Ant Design、Naive UI 的表单标签识别；筛选输入框的唯一 placeholder 可作为可靠定位器；点击下拉框文本时自动提取可交互控件而非内部展示 `span`。
- 修复有头采集从业务子域跳转至同站点登录子域后拾取/操作工具栏消失的问题：跳转后会释放旧页面控制器并自动注入当前模式的工具栏。
- 修复有头页面元素采集中的同站点请求拦截：已通过启动校验的页面站点及其同站点资源、同站点跨子域顶层跳转不再重复执行网络校验，避免 Chrome 显示 `ERR_BLOCKED_BY_CLIENT`；跨站点跳转与非同站点请求仍维持拦截。
- 优化测试配置中的项目产品：新增稳定产品编码、负责人和启停状态；旧产品自动补齐编码并默认启用。
- 产品模块详情新增模块、环境、页面、接口、UI/API 用例统计，并禁止删除仍有关联模块、环境或 UI 用例的产品。
- 支持复制产品基本信息及两级模块；不复制环境、页面、接口和用例，新产品默认启用。
- 停用产品后禁止新建 UI 测试用例和测试环境，既有资产与历史数据仍可查询。
- 项目产品支持按项目分组的卡片视图与紧凑表格视图切换。
- 优化产品与模块维护界面：强化产品编码、负责人、状态层级，并以紧凑资产摘要展示模块关联数据。
- 重构项目产品工作台视觉层级：项目分组采用更清晰的识别条、产品卡片与无障碍焦点反馈。
- 调整产品卡片为高密度资源列表样式，移除重阴影、渐变和位移动效，提升后台管理场景的扫读效率。
- 优化产品模块配置：支持一级、二级模块树下钻筛选，并重构模块导航与列表工作区层级。
- 新增和编辑模块时，一级、二级模块改为联动下拉选择。
- 新增模块未选择一级、二级模块时，模块名称自动作为一级模块保存。
- 产品模块取消一级、二级、三级概念，统一为模块名称与可选上级模块。
- 修复候选项已处理后仍显示在审核列表的问题：界面会自动移除过期候选；服务端不再附带无关的页面元素名称重复提示。
- 修复有头采集页面刷新后拾取/操作工具栏消失的问题：框架导航即使 URL 未变化，也会清理失效控制器并重新注入工具栏。
- 修复有头采集把所有请求误判为顶层导航的问题：现在正确调用 Playwright 的导航判定方法，同站点登录网关等 XHR 请求可按会话级页面地址授权放行。
- 修复有头采集在页面同源路由切换后，操作模式仍会错误拾取元素的问题：重新注入拾取器前会主动移除旧脚本及其事件监听。
- 调整有头页面元素采集的私网访问授权：不再依赖执行器静态私网白名单；采集会话会根据目标页面地址，临时允许同一站点的子域名请求，停止会话后自动失效。

- 修复有头采集操作模式仍错误拾取元素的问题：不再依赖可能残留的 Alt 键状态，切换模式时会清空临时修饰键状态。
- 优化框架组件按钮定位：原生按钮自动使用 role/text 语义定位，并识别带命名空间的稳定业务类；统一认证页“登录”按钮可生成唯一且评分不低于 70 的定位器。
- 修复表单标签定位器评分与计数：定位器限定在最近表单项内，并使用实际 XPath 计数回写；统一认证页账号、密码输入框均可生成评分 75 的唯一定位器。
- 修复页面元素拾取被错误丢弃的问题：采集脚本与安全复核脚本统一处理 `autocomplete` 属性，账号、密码输入框可正常上报候选。
- 修复统一认证登录页的账号、密码输入框采集：支持从 Element UI 表单项中的文本标签识别上下文；候选审核区新增单条删除。
- 优化页面元素采集定位：为同组件表单控件自动结合表单标签及 `name`、`aria-label`、`placeholder`、`autocomplete` 生成候选定位器，并实测唯一性。
- 修复有头页面元素采集时浏览器被手动关闭后平台仍显示“采集中”的问题：执行器会清理本地会话并立即向平台回报失败状态，下一次可重新启动浏览器采集。

- 关闭页面元素采集审核抽屉时会同步提交停止命令，确保执行器关闭当前浏览器后再进入下一次有头采集。

- 修复执行器重启后未继承平台与本地回调配置的问题，确保有头页面元素采集使用包含命令轮询能力的新版本执行器。

- 修复页面元素有头采集启动弹窗未展示在线 UI 执行器的问题，兼容执行器列表接口的数组响应。

- 补齐页面元素自动采集生命周期清理与接口文档：平台每分钟删除到期候选、清空终态会话令牌摘要，API 退出时取消后台调度并优雅关闭；明确平台/执行器接口、权限、状态机、容量、安全边界及当前发布阻断项。

- 页面元素配置工作台新增“自动采集”入口，支持选择在线 UI 执行器启动有头采集、在审核抽屉处理候选并于保存后刷新列表；元素版本界面展示采集来源、URL、标签、可访问名称、质量评分和最近验证时间，并支持确认后以新版本方式回滚。

- 修复页面元素采集审核的多目标冲突与轮询边界：后端现返回全部同指纹目标 ID/名称，多目标更新必须显式选择；启动器仅接受在线 UI 执行器，忽略项不再受名称/定位器门禁影响，仅活动或已完成会话可保存。终态候选会分页接收至短页或总数耗尽，网络/5xx 与鉴权、缺失、业务错误采用不同恢复策略，且保存响应缺项或包含未知 ID 时不再误报成功。
- 新增页面元素采集启动与候选审核工作台：支持在线 UI 执行器与 Chrome/Edge 有头启动、2 秒增量轮询和断网退避、候选质量轨道、名称内联审核、重复冲突三种决策及最多 200 项原子批量入库；关闭、切换会话或卸载会取消请求，409 字段问题可回填并聚焦对应候选，界面不展示会话凭据、指纹或敏感 URL。
- 完成有头页面元素采集的最终安全收口：页面绑定增加随机 nonce、来源/frame/origin 复核与单飞限流，截图改为无 DOM 注入的 clip + mask；所有浏览器请求执行 DNS/SSRF 门禁，平台回调禁止凭据随重定向外泄。启动 ACK 可按原 receipt 幂等重试，候选以 `clientCaptureId` 实现数据库级幂等，401、poller、GUI 与应用关闭统一清理凭据、浏览器和临时文件。
- 新增执行器有头 Chrome/Edge 页面元素拾取：以独立单会话 BrowserContext 处理平台租约命令，支持 pick/operate、Alt、Esc、open Shadow DOM 和同源 iframe；候选经过 DOM allowlist、前后敏感扫描、可信定位器复核及局部截图遮罩，401/409、停止、过期、失败与应用关闭会彻底清理浏览器和临时文件。
- 加固执行器元素采集定位器生成：候选快照与回传会脱敏 URL、敏感属性和值，未知匹配数量不再被误判为唯一，并强化定位器排序、转义、动态标识过滤、指纹稳定性和平台载荷门禁。
- 新增执行器页面元素候选定位器生成器：基于脱敏 DOM 快照生成最多三组稳定定位器、质量评分和 SHA-256 指纹，并过滤动态标识与敏感输入数据。

- 关闭采集启动命令状态机绕过：仅有效 leased start 回执可原子激活并 ACK，会话在命令 queued、expired、缺失或租约失效时返回冲突且不更新；ACK 后的后续 heartbeat 无需回执，并补齐真实 PostgreSQL 历史迁移、命令清理与事务回滚验证。
- 首次采集 heartbeat 现在必须携带当前 start 租约回执，并在同一数据库事务内激活会话和确认启动命令；后续 heartbeat 在启动命令已确认后无需重复回执。
- 加固采集命令租约：修复 PostgreSQL `RETURNING` 结果集未关闭导致的连接忙，命令领取、会话过期和后台清理改为独立、安全的事务流程；旧命令载荷中的令牌会在迁移中移除。
- 修复采集命令令牌泄露：启动命令不再持久化会话明文令牌，执行器领取短租约时才生成并轮换令牌摘要；新增命令确认接口，未确认的停止/过期命令可在租约到期后重投。
- 加固页面元素采集命令：命令改为 PostgreSQL 持久化队列并以 `FOR UPDATE SKIP LOCKED` 原子领取；轮询使用执行器长期 `X-Executor-Token`，启动命令一次性下发会话令牌。创建、模式切换、停止和超时命令与会话状态在同一事务中提交。
- 收紧采集会话的页面 owner/admin 访问控制，并使候选项 `no-store` 覆盖 JWT 认证失败响应；页面资产继续使用实际的 `asset_type='page'`，不影响 `page_element` 元素列表。
- 加固页面元素采集 API：会话、候选和元素版本改为页面 owner/admin 资源级授权；执行器回调使用 Bearer 会话令牌认证，候选响应禁止缓存，版本回滚完整保留快照字段。
- 新增页面元素自动采集的平台与执行器 API，支持采集会话、候选审核、元素版本查询及以递增新版本形式执行的回滚。
- 补齐采集候选审核的锁前/锁后目标变化、候选输入边界、软删除重复识别、历史迁移清理及元素版本递增回归保障。
- 完成采集候选审核一致性保障：显式 `targetElementId` 优先于陈旧候选目标，锁后单一目标可自动确定，跨页、失效、重复更新与名称自排除统一使用有效目标；`create`/`ignore` 拒绝携带目标，并返回可消费的结构化候选问题。
- 为采集候选更新增加显式目标确认：多个同指纹元素存在时，审核者必须选择 update 目标，批量审核锁定会话全部候选后再执行决策。
- 调整采集候选 fingerprint 冲突决策：保留用户显式新建同指纹元素的能力，更新操作使用锁后唯一目标或要求在歧义时明确选择；活动元素名称仍受数据库唯一约束保护。
- 强化页面元素采集并发一致性：数据库阻止同页活动元素重名，采集审核会依据锁后最新冲突状态重新决策。
- 修复页面元素候选审核的 ID 与事务语义：对外以安全的整数游标定位候选，内部保留字符串主键；批量保存锁后重读最新候选并重新执行完整审核门禁。
- 加固页面元素采集候选项审核的并发安全与分页兼容性：使用独立游标而非业务 ID 分页，批量保存锁后复核候选状态，并持久化完整定位器组。
- 新增页面元素采集候选项审核与原子批量入库：校验候选指纹、可靠唯一定位器、名称和冲突决策，支持重复元素处理、版本留档、审计日志以及容量预警和全批次回滚。

- 新增页面元素自动采集会话状态机：校验页面与 Chromium 执行器条件，限制 Chrome/Edge channel 和安全 URL，支持 30 分钟会话过期、60 秒原机恢复窗口、停止幂等及一次性令牌哈希校验。
- 强化页面元素采集会话的首次心跳 browser context 绑定与后续不可变校验，并拒绝不安全或相对的最终页面地址。

- 补齐页面元素自动采集会话恢复、浏览器通道、候选冲突审核和页面元素元数据读取所需的数据基础。
- 新增页面元素自动采集的数据模型、数据库迁移和 `ui.element.read`、`ui.element.capture`、`ui.element.manage` 权限目录，为后续会话采集与候选审核提供基础。
- 调整接口管理列表列宽分配并收紧操作列专属间距，确保调试、编辑、删除按钮及选择框完整位于所属单元格可见区域。
- 修复接口管理操作列被文本裁切和响应式判断依赖 viewport 的问题，按列表实际内容宽度切换筛选布局，并补充选择框可访问名称及截断字段键盘/鼠标气泡验证。
- 优化接口管理的单行搜索与自适应列表：桌面端筛选和操作区保持同一行，列表不再产生横向溢出，窄屏自动切换为单列筛选。
- 修复系统概览因近 7 日执行趋势 SQL 使用关键字别名而无法查询的问题。
- 新增系统运行概览与操作审计工作台，支持 7 日执行趋势、执行器在线态势、最近管理动作、日志组合筛选、分页及 CSV 导出，并修复本地启动配置被遗留环境变量覆盖的问题。
- 统一浏览器 100% 缩放下的页面滚动规范：纵向溢出由内容容器滚动，列表采用固定列宽，超长字段省略并提供可悬停、可键盘聚焦的完整内容气泡。
- 新增系统参数与通知默认策略工作台，支持配置版本历史、回滚、执行调度参数实时生效、连续登录失败锁定与管理员解锁，以及系统默认通知与个人覆盖。
- 移除业务页面顶部的通用标题与说明区域，保留顶栏面包屑定位，减少重复信息和无效纵向占用。
- 全面升级前端界面设计：新增五套工作台主题，补齐统一页面标题与可访问性反馈，优化接口管理的筛选及批量操作，并将接口测试用例编辑器重构为响应式三栏工作台。
- 重构前端工程控制台的视觉层级与交互反馈，统一导航、页面标题、数据卡片、按钮、表单、表格和模态框样式，并补充键盘“跳到主要内容”入口及移动端适配。
- 新增系统管理权限底座，支持角色权限实时加载、系统管理接口 403 鉴权及前端菜单权限过滤。
- 新增用户生命周期和角色权限矩阵，支持一次性临时密码、首次登录强制改密、密码重置、角色复制与权限变更即时失效。
- 优化本地后端启动流程，支持从 Git 忽略的 `.env.local` 自动读取数据库配置。
- 新增每日更新策略，要求每次修改或新增功能时记录变更内容、影响范围和验证结果。
- 新增界面自动化测试用例专用后端模块。
- 新增 `/api/test-cases` REST API，覆盖 CRUD、详情、参数化数据集、导入和导出。
- 新增 `test_cases`、`test_case_steps`、`test_case_datasets` 数据库表和初始化逻辑。
- 新增测试用例 Service、Controller、Repository 测试。
- 前端“界面自动化 / 测试用例”切换到专用 API。
- 前端新增测试用例筛选、分页、新增、编辑、删除、批量删除、导入、导出、详情和参数化数据 UI。
- 前端新增 Vitest 组件测试、Playwright 关键 E2E 和覆盖率统计配置。
- 新增 `SPEC.md`、`tasks/test-case-module.md`、`docs/api-test-case.md`、`PR_DESCRIPTION.md`、`SUMMARY.md`。
