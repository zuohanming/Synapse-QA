# 项目协作规则

## 每日更新

每次修改代码、配置、文档，或新增功能时，必须同步记录每日更新。

1. 在 `docs/daily-updates/YYYY-MM-DD.md` 中追加本次变更；文件不存在时，复制同目录的 `TEMPLATE.md` 创建。
2. 每条记录必须包含变更内容、影响范围和验证结果；未执行验证时，应明确写明原因，不得省略。
3. 同一天的多次变更追加到同一文件，不覆盖已有记录。
4. 面向使用者的功能新增、行为变化、缺陷修复或不兼容变更，还必须同步更新根目录 `CHANGELOG.md` 的 `Unreleased` 章节。
5. 纯内部操作且未改变仓库文件内容时，无需记录。

每日更新与对应变更应在同一次任务中完成，视为功能或修改的完成条件之一。
