# 2026-08-13 每日更新

## 优化：节点操作选择器改为模态框

- 变更内容：将页面步骤工作台右侧配置面板的“节点操作”窄 cascader 下拉重构为居中模态框：左侧为操作分类树导航，右侧展示当前分类下的全部操作项，适配几十上百个操作的场景。视觉上区分分类标题（加粗置灰、色点标识）与可点击项，操作项支持 hover 高亮、键盘高亮与当前选中态三级区分。选中后输入框回填文本保持现有 `${操作分组} / ${操作名}` 格式（沿用 operationGroup/operationName 字段），后端与导出配置不变。支持键盘导航：分类树 ↑/↓ 切换、Enter 进入；操作列表 ↑/↓ 移动高亮、Enter 确认、Esc 关闭；焦点进入模态框。采用懒渲染，仅在打开模态框且选中某分类后渲染该分类的操作列表，长列表由滚动容器承载，不一次性渲染所有分组。附带修复节点点击未选中（配置面板不出现）的回归。
- 影响范围：`frontend/src/pages/UIAutomationPage.js`、`frontend/src/styles/global.css`、`frontend/src/pages/UIAutomationPage.test.js`。
- 验证结果：`npm run test -- UIAutomationPage.test.js` 32 项通过；`npm run build` 成功，仅保留既有 chunk size 提示；`git diff --check` 通过。

## 新增 DeepSeek 极简主题并设为默认

- 变更内容：新增「DeepSeek 官网风格」浅色主题作为默认主题落地。在 `appearance.css` 新增 `:root[data-theme="deepseek"]` 主题变量块，将 DeepSeek 设计令牌映射到现有变量体系（主蓝 #3964FE、hover 变亮 #5686FE、近黑文字 #0F1115、蓝灰次级文字 #61666B、极浅蓝灰背景 #F7F8FA、细边框 rgba(0,0,0,.08)、柔和阴影与 2px 品牌蓝焦点环等），并补充圆角/阴影/焦点环变量；末尾新增 `[data-theme="deepseek"]` 组件级覆盖（胶囊实心主按钮 + hover 变亮、幽灵次按钮、16px 卡片、10px 输入框、轻量表头、导航 hover 6% 蓝灰、2px 品牌蓝焦点环、登录页蓝灰化）；主题选择网格改为 6 列并新增 `theme-preview-deepseek` 预览色块。`appearance.js` 的 `defaultAppearance.theme` 由 `"blueprint"` 改为 `"deepseek"`。`SystemPage.js` 主题选项数组新增 `["deepseek","DeepSeek 极简","蓝灰极简、圆润留白，致敬 DeepSeek 官网"]`，并将 blueprint 描述由「默认工作台」改为「经典工作台」。
- 影响范围：`frontend/src/styles/appearance.css`、`frontend/src/utils/appearance.js`、`frontend/src/pages/SystemPage.js`。
- 验证结果：`npm run test` 全量 85 项中 82 项通过、3 项失败（`ConfigPage.test.js` 执行器配置页）；经 `git stash` 暂存本次改动后复跑，该 3 项仍失败，确认与本次主题改动无关、为预先存在的失败。`npm run build` 成功，仅保留既有 chunk size 提示。

## DeepSeek 极简主题骨架精修（第二阶段）

- 变更内容：在 `appearance.css` 的 `[data-theme="deepseek"]` 作用域内补充骨架级精修，让应用外壳贴合 DeepSeek 官网观感。桌面端侧边栏宽度调整为 250px（`@media (min-width: 901px)` 限定，移动端沿用单列布局）；品牌区改为 64px 高 + 细分隔线、logo 去旋转与青绿斜纹装饰（`brand-mark` 改为简洁圆角方块、`::before/::after/span` 隐藏）、品牌文字层级用蓝灰；菜单项尺寸与圆角克制（`menu-button` 40px/`menu-child` 36px、12px 菜单圆角、chevron 蓝灰）；顶栏高度统一 64px、面包屑与用户信息用蓝灰次级文字；页面头部 eyebrow 用品牌蓝、标题近黑、描述蓝灰；补充 hover 查漏，分页、通知抽屉头部与列表项、弹窗关闭按钮统一为 6% 蓝灰微变。所有精修均在 deepseek 作用域内，未改动 Layout.js 的 JSX 结构与 global.css（避免波及其它主题）。
- 影响范围：`frontend/src/styles/appearance.css`（`[data-theme="deepseek"]` 作用域新增骨架精修块）。
- 验证结果：`npm run build` 成功，仅保留既有 chunk size 提示；`npm run test` 全量 85 项中 82 项通过、3 项失败（仍为 `ConfigPage.test.js` 执行器配置页的已知预先存在失败，与本次改动无关），`AppearanceSettings.test.js` 与 `NotificationCenter.test.js` 均通过，未新增失败。

## 补齐执行器卡片样式（DeepSeek 主题）

- 变更内容：另一任务在 `ConfigPage.js` 恢复了执行器列表管理功能，但所用 `executor-*` 卡片类名无样式定义。在 `appearance.css` 末尾补齐通用布局（放通用作用域，随主题变量适配）+ DeepSeek 精修。通用布局覆盖：`.executor-status-panel .panel-header`（标题栏底边框）、`.panel-description`、`.executor-card-grid`（响应式 `repeat(auto-fill, minmax(min(320px,100%),1fr))`）、`.executor-card`（纯白卡片 + 左侧 3px 状态色条 `::before`，修饰类 is-online/is-pending/is-offline）、`.executor-card-head`/`.executor-name-cell`/`.executor-icon`、`.executor-state`（状态徽章小胶囊 + 圆点，绿/琥珀/灰）、`.executor-load-grid`（运行中/排队中/最大并发三列）、`.executor-card-detail`/`.executor-endpoint`、`.executor-card-footer`/`.executor-capabilities`（支持类型小胶囊）、`.executor-token-zone`/`.executor-token-actions`/`.executor-token-result`（浅灰 token 区 + 等宽 token + 复制按钮）、`.executor-empty`（空状态）、`.executor-create-modal`/`.executor-create-hint`（新增执行器弹窗）、`.spin-icon`（旋转动画）。DeepSeek 精修将卡片圆角升到 16px + 柔和阴影、hover 描边用 `--primary-border`。全部用 CSS 变量引用，其它五套主题用通用布局自动适配、不因本次改动错乱。
- 影响范围：`frontend/src/styles/appearance.css`（末尾新增执行器卡片布局 + DeepSeek 精修）。
- 验证结果：`npm run test` 全量 13 个文件、85 项全部通过（`ConfigPage.test.js` 3 项已通过，功能由另一任务修复）；`npm run build` 成功，仅保留既有 chunk size 提示。

## AI 工作台 DeepSeek 精修（第三阶段）

- 变更内容：在 `appearance.css` 的 `[data-theme="deepseek"]` 作用域内为 AI 智能工作台的 `ai-*` 类名写覆盖，替换紫色系/绿色系硬编码为品牌蓝 + 蓝灰文字 + 柔和状态色。覆盖页面头部与标题（标题近黑、icon 品牌蓝）、能力卡片（16px 圆角 + 柔和阴影、图标 `!important` 覆盖内联紫/绿为品牌蓝）、能力徽章（规划中/开发中/已完成 → 柔和琥珀/品牌蓝/柔和绿）、Mock 对话与代码预览、总览统计（三图标用 `:nth-child` 区分品牌蓝/琥珀/绿，`!important` 覆盖内联色）、模型卡片（16px 圆角 + 柔和阴影 + 蓝灰文字，已启用柔和绿描边）、设置区块、开关与输入组、对话界面（用户头像实心品牌蓝、助手头像浅蓝、消息气泡蓝灰、错误/工具消息柔和状态色）、输入态（光标/打字指示/输入区）。`.ai-model-card-provider` 的服务商标识色（OpenAI 绿、DeepSeek 紫等）属第三方品牌标识，予以保留。未改 `AIPage.js` 的 JSX 与 `AIPage.css`（其硬编码均可由 deepseek 作用域覆盖，且改动会波及其它主题）。
- 影响范围：`frontend/src/styles/appearance.css`（末尾新增 AI 工作台 deepseek 精修块）。
- 验证结果：`npm run build` 成功，仅保留既有 chunk size 提示；`npm run test` 全量 13 个文件、85 项全部通过（首跑曾出现 `ElementCaptureDrawer.test.js` 分页 fetch 次数断言 1 项 flaky 失败，单独重跑 21 项全通过，再全量复跑 85 项全通过，确认与本次改动无关）。

## 数据工厂 / 执行报告 / 接口调试器 DeepSeek 精修（第三阶段续）

- 变更内容：在 `appearance.css` 的 `[data-theme="deepseek"]` 作用域内为三个页面写覆盖。数据工厂（`df-*`）：搜索框 10px 圆角 + 品牌蓝焦点环、卡片 16px 圆角 + 柔和阴影、灰文字（#333/#888/#999/#1a1a1a 等）→ 蓝灰变量、语法/示例/预览浅灰底 → `--panel-muted`/`--primary-soft`。执行报告（`report-*`）：统计卡与报告弹窗的深蓝渐变（#172033/#24334d）替换为品牌蓝渐变（#2f4bd6→#3964fe→#5686fe）、hero 文字浅蓝灰调整、统计卡/区块/评分卡 16px 圆角 + 柔和阴影；结果分布条、失败定位、状态徽章等业务语义色（通过绿/失败红/跳过琥珀）保留。接口调试器（`api-*`）：`api-tag.method-patch` 装饰性紫色（#6556d9）替换为品牌蓝，其余方法色（GET 青/POST 橙/PUT 琥珀/DELETE 红）与 endpoint/protocol 标签色属语义色予以保留；发送按钮胶囊圆角。未改三个页面的 .js 与其它主题规则。
- 影响范围：`frontend/src/styles/appearance.css`（末尾新增三个页面 deepseek 精修块）。
- 验证结果：`npm run build` 成功，仅保留既有 chunk size 提示；`npm run test` 全量 13 个文件、85 项全部通过，未新增失败。

## 页面步骤画布工作台 DeepSeek 精修（第三阶段末批）

- 变更内容：在 `appearance.css` 的 `[data-theme="deepseek"]` 作用域内为页面步骤画布工作台的 `flow-*`/`node-port`/`zoom-indicator`/`minimap`/`operation-modal-*` 等类名写覆盖。工作台外壳与调色板（`step-workbench` 16px 圆角、`palette-group`/`palette-item` 圆角 + 蓝灰文字 + 浅灰底）、画布背景（浅灰 + 蓝灰点网格，替换 #dce7f5 蓝点）、空画布（蓝灰文字 + 虚线边框）、节点卡片（8px 圆角 + 柔和阴影 + 蓝灰文字，保留 `node.color` 内联左侧色条与业务语义）、连线与分支选择（分支真假色保留语义）、端口与流统计（warning 徽章柔和化）、缩放指示器与小地图（8px 圆角 + 柔和阴影 + 品牌蓝小地图块）、操作选择器触发器与模态框（16px 圆角 + 柔和阴影 + 6% 蓝灰 hover + 品牌蓝选中态）。节点类型色（`node.color`）、分类色点（`group.color`）、调色板色点（`item.color`）等内联业务语义色全部保留。未改 `UIAutomationPage.js` 的 JSX 结构与其它主题规则。
- 影响范围：`frontend/src/styles/appearance.css`（末尾新增画布工作台 deepseek 精修块）。
- 验证结果：`npm run build` 成功，仅保留既有 chunk size 提示；`npm run test` 全量 13 个文件、85 项全部通过，未新增失败。

## 修复画布节点文字模糊（字号/对比度/字重）

- 变更内容：用户反馈画布节点卡片文字模糊。在 `appearance.css` 的 `[data-theme="deepseek"]` 作用域内提高节点文字字号与对比度：`.flow-node span/small` 字号 9px→10px、加 `font-weight:500`、颜色保持 `--muted`；`.flow-node strong` 字号 11px→12px、加 `font-weight:600`、颜色保持 `--text`；`.flow-node em`（"已配置/未保存"状态）字号 10px→11px、`font-weight:600`、颜色由对比度不足的 `--text-tertiary`(#81858C) 改回 `--muted`(#61666B)。保留 `node.color` 左侧语义色条与节点 8px 圆角/柔和阴影不变；字号提升幅度控制在 156px 宽节点内不撑破（min-height 64px 有缓冲，节点轻微增高至约 66px，不影响画布自由定位布局）。未改 `UIAutomationPage.js` 与 global.css 基础字号（仅修默认 DeepSeek 主题，避免波及其它五套主题）。
- 影响范围：`frontend/src/styles/appearance.css`（修改 `[data-theme="deepseek"]` 下 `.flow-node` 文字覆盖）。
- 验证结果：`npm run build` 成功，仅保留既有 chunk size 提示；`npm run test` 全量 13 个文件、85 项全部通过（首两次运行曾出现 vitest forks worker 内存不足/启动失败的瞬时环境问题，重跑后恢复，确认与本次改动无关）。

## 优化：节点操作选择器改回下拉面板

- 变更内容：将节点操作选择器从居中模态框（遮罩 + `role=dialog` + 标题栏）改回锚定「节点操作」触发框下方的下拉面板。去掉全屏遮罩、居中与标题栏，面板在触发框正下方展开（左对齐），内部保留左侧分类树 + 右侧操作列表结构、分类色点、操作项 hover/选中/键盘高亮三级区分与懒渲染；键盘导航（分类树 ↑/↓、操作列表 ↑/↓/Enter、Esc）与打开后焦点进入面板保持不变；关闭方式改为点击面板外部、Esc 或选中后关闭。回填文本、`selectOperation` 写回字段与参数区渲染不变。样式上删除 `.operation-modal-*`，新增 `.operation-dropdown/.operation-dropdown-panel/.operation-dropdown-body`，并同步 `[data-theme="deepseek"]` 作用域覆盖；测试改用 `role="listbox"`/`role="treeitem"` 定位，未删减断言。
- 影响范围：`frontend/src/pages/UIAutomationPage.js`、`frontend/src/pages/UIAutomationPage.test.js`、`frontend/src/styles/global.css`、`frontend/src/styles/appearance.css`。
- 验证结果：`npm run test` 全量 13 个文件、85 项全部通过（含 32 个 UIAutomationPage 测试）；`npm run build` 成功，仅保留既有 chunk size 提示；`git diff --check` 通过。
