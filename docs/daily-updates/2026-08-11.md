# 2026-08-11 每日更新

## 列表行内操作元素定位兼容

- 变更内容：页面元素采集对表格或列表行内重复的按钮、链接生成“唯一行锚点 + 行内操作”的组合 XPath；支持原生表格、ARIA row、Element UI 与 Ant Design 表格行。
- 影响范围：执行器的页面元素采集定位生成与候选项质量评分。
- 验证结果：执行 `python -m pytest .\\tests\\test_locator_generator.py .\\tests\\test_element_picker.py -q`，结果为 72 passed、1 skipped。

## 自定义表格行兼容补充

- 变更内容：补充通过同级重复结构和稳定行 class 识别自定义表格行，覆盖 `yv-*` 等非原生表格组件；行内业务编号可作为唯一锚点。
- 影响范围：页面元素采集在自定义表格中的“编辑、删除”等重复操作按钮定位。
- 验证结果：执行 `python -m pytest .\\tests\\test_locator_generator.py .\\tests\\test_element_picker.py -q`，结果为 73 passed、1 skipped；执行器重启后健康检查返回 HTTP 200。

## AI 智能功能迁移

- 变更内容：从另一工作树选择性迁移 AI 模型配置、连接测试、SSE 对话、测试资产工具调用以及 AI 智能工作台页面；保留当前页面元素采集和执行器模块，不合并其删除变更。
- 影响范围：后端新增 `/api/ai` 配置、模型和对话接口；前端新增 AI 智能一级菜单及五个二级入口。
- 验证结果：执行 `go test ./cmd/api ./internal/controller ./internal/router ./internal/service` 通过；执行 `npm run build` 通过。
