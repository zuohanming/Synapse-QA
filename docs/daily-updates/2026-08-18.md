# 2026-08-18 每日更新

## 完成性能测试 P2 基线、趋势、导出、定时与多环境

- 变更内容：性能测试方案复用现有 `test_objects` 环境及 Base URL，支持方案默认环境和单次执行环境覆盖，并将原始配置与解析后的 URL/混合场景写入不可变执行快照；新增 P99 指标全链路、通过执行设为基线、P95/P99/错误率/RPS 差值与变化率、同方案同场景同环境趋势，以及严格超过 10% 的劣化标记和站内通知补偿。报告支持脱敏 CSV/JSON 下载，CSV 使用固定 30 列、UTF-8 BOM、RFC4180 和公式注入防护。新增五字段 Cron + IANA 时区定时规则，使用数据库 lease、`FOR UPDATE SKIP LOCKED`、确定性幂等键和活动执行跳过保证重启及多实例下不重复触发；前端新增环境选择、基线对比、趋势、导出和定时规则管理页面。
- 影响范围：后端性能测试迁移、model/repository/service/controller/router、通知去重与 `robfig/cron/v3` 依赖；执行器 P99 回调；前端性能方案、执行确认、方案详情、运行详情、定时规则、service、路由、菜单、工具函数与测试。
- 验证结果：后端 `go build ./...`、`go vet ./...`、`go test ./...` 全部通过；执行器 `python -m pytest tests/ -q` 为 `208 passed, 2 skipped`；前端 `npm run test` 为 125 项通过，`npm run build` 通过（仅既有 bundle chunk size 提示）；`git diff --check` 通过。真实环境 142/143 分别完成环境解析运行，runs 83/84 验证默认与覆盖环境快照；baseline 1 与 fixture run 33 验证四指标超过 10% 劣化；schedule 1 真实一分钟触发 run 85 且同 occurrence 仅一条，跨环境活动执行正确记为 `skipped_active_run`；run 86 的 CSV/JSON 导出未命中 Body、Query 或诊断中的敏感原值；劣化通知删除后由补偿扫描恢复且始终仅一条。

## 优化性能测试定时规则页面

- 变更内容：以“压测方案管理”页面为直接参照重构定时规则页面骨架。最终确认大块留白的根因是蓝色主题将 `.workspace > .section-stack` 首行设为 `minmax(0, 1fr)`，而定时规则页存在两个直属 `resource-panel`，导致第一个筛选面板被强制撑满工作区。现将直属面板严格收敛为一个，把 `panel-header`、`filter-grid`、`list-actions`、通知、`StateBlock`、`TablePanel` 和 `PaginationBar` 全部放入同一面板，与压测方案页层级一致；删除独立 Hero、H1、统计摘要、第二个列表面板及其高度覆盖。筛选调整为“方案/状态 + 搜索/重置”的提交式交互，列表复用相同的表格字号、行高、圆角和阴影。保留 Cron、结果状态和紧凑空态的必要局部样式，并改善规则表单说明；左侧菜单补充日历时钟图标。
- 影响范围：`frontend/src/pages/PerfSchedulesPage.js`、`frontend/src/pages/PerfSchedulesPage.test.js`、`frontend/src/components/Layout.js`、`frontend/src/styles/global.css`。
- 验证结果：前端 `npm run test` 20 个测试文件、126 项测试全部通过；`npm run build` 通过，仅保留既有 bundle chunk size 提示；`git diff --check` 通过。

## 补齐定时规则空数据分页器

- 变更内容：移除定时规则列表对 `PaginationBar` 的数据行数条件限制，使分页器与压测方案页保持一致并始终显示；无规则时展示“共 0 条”、当前第 1 页、禁用的上一页/下一页和每页条数选择器，有数据时继续支持翻页及切换每页条数后回到第 1 页。
- 影响范围：`frontend/src/pages/PerfSchedulesPage.js`、`frontend/src/pages/PerfSchedulesPage.test.js`。
- 验证结果：新增空数据分页导航、总数、当前页、禁用按钮和每页条数选择器断言；前端 `npm run test` 20 个测试文件、126 项测试全部通过，`npm run build` 通过，仅保留既有 bundle chunk size 提示。

## 新增性能测试模板操作手册

- 变更内容：新增面向测试人员和管理员的性能测试模板操作手册，完整说明压测方案、测试报告和定时规则的使用流程；覆盖 baseline、ramp、peak、stress、soak、mixed 六类场景，环境与 URL 解析、请求与阈值配置、冒烟请求、执行确认、实时监控、状态判读、基线比较、脱敏导出、Cron 调度、推荐模板、安全建议和常见问题。
- 影响范围：`docs/performance-testing-user-guide.md`、`CHANGELOG.md`。
- 验证结果：分别对照前端页面及后端/执行器实现完成事实核对，修正 ramp/stress 约束、无环境 URL 建议、可导出状态、基线环境维度和脱敏归档边界；文档内部链接和 Git 空白错误检查通过。

## 性能测试支持从接口管理一键添加请求

- 变更内容：压测方案普通场景的请求配置新增“从接口管理添加”，可按当前产品、关键词和 Method 查找接口，单选后确认覆盖 Method、URL、Params、Headers 和 Body；mixed 场景支持多选追加，按 50 条总上限限制本次选择数量，默认权重为 0.1，并自动为重名接口添加序号后缀。HEAD、OPTIONS 等不受压测支持的方法不可选择；导入内容作为可独立编辑的方案快照，不与接口管理持续联动。
- 影响范围：`frontend/src/components/PerfInterfacePicker.js`、`frontend/src/components/PerfInterfacePicker.test.js`、`frontend/src/pages/PerfPlanEditPage.js`、`frontend/src/pages/PerfPlanEditPage.test.js`、`frontend/src/styles/global.css`、`docs/performance-testing-user-guide.md`、`CHANGELOG.md`。
- 验证结果：前端 `npm run test` 21 个测试文件、129 项测试全部通过；`npm run build` 通过，仅保留既有 bundle chunk size 提示；最终 Git 空白错误检查通过。

## 修复性能冒烟请求不显示 HTTP 响应

- 变更内容：执行器的 smoke k6 脚本不再丢弃 `http.request` 返回对象，现通过专用响应 marker 回传 HTTP 状态码、耗时、Content-Type、响应体大小、脱敏响应头和响应体。文本响应在 k6 采集阶段即按 256 KiB 限制截断，二进制响应不写入返回内容；Python 结果归一化继续执行二次限长和脱敏。前端新增新旧结果结构兼容，展示响应摘要、可折叠 Headers、格式化 JSON/文本 Body，以及错误、截断和二进制提示。
- 影响范围：`executor/app/runners/perf_runner.py`、`executor/tests/test_perf_runner.py`、`frontend/src/pages/PerfPlanEditPage.js`、`frontend/src/pages/PerfPlanEditPage.test.js`、`frontend/src/styles/global.css`、`docs/performance-testing-user-guide.md`、`CHANGELOG.md`。
- 验证结果：执行器 `python -m pytest -q tests` 为 `218 passed, 2 skipped`，`py_compile` 通过；使用本机 k6 v2.2.0 对 `/api/health` 执行真实 smoke，退出码为 0，成功解析 HTTP 200、24 字节响应体并清理临时目录；前端 `npm run test` 21 个测试文件、132 项测试全部通过，`npm run build` 通过，仅保留既有 bundle chunk size 提示；`git diff --check` 通过。

## 修复打包执行器周期性闪烁 DOS 窗口

- 变更内容：新增跨平台子进程启动参数工具，在 Windows 下统一组合 `CREATE_NO_WINDOW` 与隐藏 `STARTUPINFO`；心跳每 10 秒执行的 `k6 version`、性能任务的 k6 版本检测和实际 k6 进程、script runner、pytest runner、Playwright Python 节点均改为隐藏启动。性能任务仍保留 `CREATE_NEW_PROCESS_GROUP`、`CTRL_BREAK_EVENT` 优雅停止和强制终止兜底，Playwright 有头浏览器的可见语义不变。
- 影响范围：`executor/app/core/subprocess_utils.py`、`executor/app/services/heartbeat_client.py`、`executor/app/runners/perf_runner.py`、`executor/app/runners/script_runner.py`、`executor/app/runners/pytest_runner.py`、`executor/app/runners/playwright_runner.py`、`executor/tests/test_subprocess_utils.py`。
- 验证结果：执行器 `python -m pytest -q tests` 为 `226 passed, 2 skipped`，相关文件 `py_compile` 和 `git diff --check` 通过；PyInstaller 全量清理打包成功，新包启动后持续运行 32 秒并监听 `127.0.0.1:8091`，覆盖三个默认 10 秒心跳周期。产物 SHA-256 为 `35FC1213E58A31A204578329D9B96526AA40AAFF1AA60F8AA4AA3BC1D415553D`。

## 修复桌面启动执行器未上报性能测试能力

- 变更内容：修复 Windows 从桌面启动打包执行器时环境变量未包含 k6 安装目录，导致心跳移除 `perf` 能力并提示“执行器不支持性能测试”。新增统一 k6 定位逻辑，优先使用 PATH，并回退检查 Program Files、Program Files (x86)、WinGet、Chocolatey 和 Scoop 标准安装位置；心跳检查、支持类型、版本探测及实际性能任务统一使用同一定位结果，同时继续隐藏 k6 子进程窗口。
- 影响范围：`executor/app/core/subprocess_utils.py`、`executor/app/services/heartbeat_client.py`、`executor/app/runners/perf_runner.py`、`executor/tests/test_subprocess_utils.py`、`executor/tests/test_perf_runner.py`、`docs/performance-testing-user-guide.md`、`CHANGELOG.md`。
- 验证结果：执行器 `python -m pytest -q tests` 为 `232 passed, 2 skipped`，相关文件 `py_compile` 和 `git diff --check` 通过；PyInstaller 全量打包成功，新包在线监听 `127.0.0.1:8091`，后端登记 `supportedTypes` 包含 `perf` 且 `checks.k6=true`；通过平台 `/api/perf/smoke` 向新包发送真实请求，任务状态为 `success`，返回 HTTP 200 和 24 字节响应体。产物 SHA-256 为 `6226375DE27E91310B4076258AB3BFDE13CEF9521CAF4DD25B61ED6A680FABFE`。

## 首页升级为项目测试工作台

- 变更内容：将原资产数量首页重构为以近期测试执行和风险处理为核心的项目概览。新增独立 `/api/dashboard/overview` 聚合接口，支持项目及 24 小时/7 天/30 天范围筛选，统一 UI、API、性能执行状态，返回成功率、失败、运行中、最近 5 条执行和最近 3 条失败事项；前端新增筛选、刷新、局部失败、加载/空态、权限快捷入口、桌面表格及窄屏执行卡片。接口与系统管理概览彻底分离，不返回用户、角色和审计信息。
- 影响范围：后端 Dashboard model/repository/service/controller、API 路由与依赖注入、Dashboard 查询索引及测试；前端 `DashboardPage`、`dashboardService`、页面测试和全局响应式样式；`docs/modules/02-首页工作台.md`、`CHANGELOG.md`。
- 验证结果：后端 `go test ./...`、`go vet ./...`、`go build ./cmd/api` 全部通过；前端 `npm run test` 为 22 个测试文件、143 项测试全部通过，`npm run build` 通过，仅保留既有 bundle chunk size 提示；真实 PostgreSQL/API 联调返回默认 `range=7d`、三来源状态均为 `ok`、执行总数 21、最近执行 5 条、失败事项总数 11 且列表限为 3 条，recent/attention UID 均唯一；项目筛选生效，非法 range 返回 400，不存在项目返回 404；`git diff --check` 通过。
