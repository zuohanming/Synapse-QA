# 2026-08-06 每日更新

## Element UI 下拉选项采集

- 变更内容：点击 Element UI 下拉选项内部 `span` 时，自动提升为 `li.el-select-dropdown__item`；生成“当前可见下拉面板 + 选项文本”的稳定 XPath。框架 class 被敏感扫描误判时仅从候选属性中移除，避免候选被丢弃。
- 影响范围：Element UI 下拉选择控件的页面元素采集、XPath 定位器生成与质量评分。
- 验证结果：执行 `node --check` 验证拾取脚本语法；执行 `D:\pyhon3.11.8\python.exe -m pytest -q tests\test_locator_generator.py tests\test_element_picker.py tests\test_capture_session_manager.py tests\test_capture_security.py`，126 passed、2 skipped。

## 采集候选分级保存

- 变更内容：候选审核从“唯一且评分 ≥70”的硬门槛改为分级保存。唯一且评分 ≥70 为可保存；唯一且评分 40–69 为待回放验证且允许保存；不唯一或评分低于 40 仍阻止保存。
- 影响范围：页面元素采集候选的质量筛选、审核提示、批量保存前端门禁和后端校验。
- 验证结果：执行 `go test ./internal/service/... ./internal/repository/...` 通过；执行 `npm test -- --run src/components/ElementCaptureDrawer.test.js`（21 passed）及 `npm run build` 通过。

## XPath 定位提取

- 变更内容：页面元素拾取脚本会根据稳定属性及按钮文本生成 XPath，并回传给定位器生成器；XPath 必定参与浏览器实际匹配数量校验和候选展示，敏感属性、动态属性和结构序号不会用于生成 XPath；唯一的稳定属性 XPath 与语义定位器不再因 DOM 深度降低评分。
- 影响范围：页面元素采集候选的定位器列表及可靠性评分。
- 验证结果：执行 `node --check` 验证拾取脚本语法；执行 `D:\pyhon3.11.8\python.exe -m pytest -q tests\test_locator_generator.py tests\test_element_picker.py tests\test_capture_session_manager.py tests\test_capture_security.py`，125 passed、2 skipped。
