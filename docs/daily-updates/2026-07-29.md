# 2026-07-29 每日更新

## 页面元素自动采集设计

- 变更内容：基于 20 项已确认决策新增页面元素自动采集设计规格，明确执行器有头 Chromium 拾取、候选审核、定位器质量门禁、重复冲突、会话安全、权限、事务入库、版本回滚和第一期技术边界。
- 影响范围：产品需求与后续界面自动化页面元素采集实施方案；本次仅新增设计文档，未修改运行时代码。
- 验证结果：已完成占位符、内部一致性、范围和歧义自检；等待使用者确认书面规格后进入实施计划。

## 页面元素自动采集实施计划

- 变更内容：将已批准设计拆分为数据模型、会话状态机、候选事务、平台 API、执行器定位器、浏览器拾取、前端审核、页面集成和端到端验收九个测试驱动任务。
- 影响范围：后续页面元素自动采集研发流程；本次仅新增实施计划，未修改运行时代码。
- 验证结果：已完成规格覆盖、占位符、类型一致性和任务依赖顺序自检。

## 页面元素自动采集数据模型与权限目录

- 变更内容：新增采集会话、候选和页面元素版本数据模型；补齐采集会话、候选和版本迁移表，以及页面元素采集元数据、活动会话执行器唯一索引、会话和候选到期索引；新增页面元素查看、采集和管理权限种子。
- 影响范围：界面自动化页面元素自动采集的数据持久化与权限目录，为后续会话、候选审核和批量保存 API 提供数据基础。
- 验证结果：`cd backend; go test ./cmd/api -run TestPermissionSeedsContainElementCapturePermissions -count=1` 通过；`cd backend; go test ./...` 通过。

## 页面元素自动采集数据模型审查修复

- 变更内容：为采集会话补充一次性令牌摘要、浏览器通道、心跳、中断和恢复截止时间；活动会话执行器唯一索引覆盖 starting、active 和 interrupted；为候选补充名称、采集 URL、重复元素和冲突处理数据；页面元素列表补回全部采集元数据扫描。
- 影响范围：会话 60 秒原机恢复、Chrome/Edge channel 限制、候选冲突审核和页面元素采集元数据读取。
- 验证结果：聚焦迁移、模型和仓储回归测试通过；`cd backend; go test ./...` 通过。

## 页面元素自动采集会话状态机与一次性令牌

- 变更内容：新增采集会话仓储与服务，创建时校验页面、在线且支持 UI 的执行器、URL、浏览器 channel 和模式；以 32 字节随机令牌的 SHA-256 摘要持久化，并仅通过 `CaptureSessionCreated` 返回一次明文令牌；实现状态切换、停止幂等、心跳恢复窗口和会话过期处理。
- 影响范围：页面元素自动采集的服务层、PostgreSQL 会话状态写入和 API 启动依赖注入；未新增 HTTP 路由。
- 验证结果：`cd backend; go test ./internal/service -run ElementCapture -count=1`、`cd backend; go test ./internal/repository -run ElementCapture -count=1` 和 `cd backend; go test ./...` 通过。

## 页面元素自动采集会话上下文绑定修复

- 变更内容：创建会话后保持 starting 状态；首次心跳必须携带非空 browser context 并原子绑定，后续心跳只能使用同一 context；同时校验创建最终 URL 和心跳 URL 均为绝对 http/https 地址。
- 影响范围：采集会话心跳服务与 PostgreSQL 条件更新，阻止跨浏览器上下文恢复和 `javascript:`、`file:`、相对地址写入会话。
- 验证结果：`cd backend; go test ./internal/service -run ElementCapture -count=1`、`cd backend; go test ./internal/repository -run ElementCapture -count=1` 和 `cd backend; go test ./...` 通过。
