# 2026-07-29 每日更新

## 页面元素采集命令持久化与认证修复

- 变更内容：将采集 start/set_mode/stop/expire 命令迁移到 `element_capture_commands` 持久化表；会话创建、模式切换、停止和超时均在同一事务内写入命令，执行器通过已有长期 `executor_token` 的 `X-Executor-Token` 轮询，并用 `FOR UPDATE SKIP LOCKED` 原子领取、过期和 24 小时留存清理。start 命令仅一次携带会话令牌，后续回调仍使用会话令牌。
- 影响范围：采集会话授权、执行器命令契约、候选项缓存控制和 PostgreSQL 启动迁移。页面访问仍为 `ui_assets.asset_type='page'` 的 owner/admin 规则；`page_element` 保持为元素列表资产类型。
- 验证结果：`cd backend; go test ./... -count=1`、`go vet ./...` 和 `git diff --check` 均通过。

## 页面元素自动采集设计

- 变更内容：基于 20 项已确认决策新增页面元素自动采集设计规格，明确执行器有头 Chromium 拾取、候选审核、定位器质量门禁、重复冲突、会话安全、权限、事务入库、版本回滚和第一期技术边界。
- 影响范围：产品需求与后续界面自动化页面元素采集实施方案；本次仅新增设计文档，未修改运行时代码。
- 验证结果：已完成占位符、内部一致性、范围和歧义自检；等待使用者确认书面规格后进入实施计划。

## 页面元素自动采集实施计划

- 变更内容：将已批准设计拆分为数据模型、会话状态机、候选事务、平台 API、执行器定位器、浏览器拾取、前端审核、页面集成和端到端验收九个测试驱动任务。
- 影响范围：后续页面元素自动采集研发流程；本次仅新增实施计划，未修改运行时代码。
- 验证结果：已完成规格覆盖、占位符、类型一致性和任务依赖顺序自检。

## 页面元素自动采集数据模型与权限目录

- 变更内容：新增采集会话、候选和页面元素版本数据模型；补齐采集会话、候选和版本迁移表，以及页面元素采集元数据、活动会话执行器唯一索引、会话和候选到期索引；新增页面元素查看、采集和管理权限种子。
- 影响范围：界面自动化页面元素自动采集的数据持久化与权限目录，为后续会话、候选审核和批量保存 API 提供数据基础。
- 验证结果：`cd backend; go test ./cmd/api -run TestPermissionSeedsContainElementCapturePermissions -count=1` 通过；`cd backend; go test ./...` 通过。

## 页面元素自动采集数据模型审查修复

- 变更内容：为采集会话补充一次性令牌摘要、浏览器通道、心跳、中断和恢复截止时间；活动会话执行器唯一索引覆盖 starting、active 和 interrupted；为候选补充名称、采集 URL、重复元素和冲突处理数据；页面元素列表补回全部采集元数据扫描。
- 影响范围：会话 60 秒原机恢复、Chrome/Edge channel 限制、候选冲突审核和页面元素采集元数据读取。
- 验证结果：聚焦迁移、模型和仓储回归测试通过；`cd backend; go test ./...` 通过。

## 页面元素自动采集会话状态机与一次性令牌

- 变更内容：新增采集会话仓储与服务，创建时校验页面、在线且支持 UI 的执行器、URL、浏览器 channel 和模式；以 32 字节随机令牌的 SHA-256 摘要持久化，并仅通过 `CaptureSessionCreated` 返回一次明文令牌；实现状态切换、停止幂等、心跳恢复窗口和会话过期处理。
- 影响范围：页面元素自动采集的服务层、PostgreSQL 会话状态写入和 API 启动依赖注入；未新增 HTTP 路由。
- 验证结果：`cd backend; go test ./internal/service -run ElementCapture -count=1`、`cd backend; go test ./internal/repository -run ElementCapture -count=1` 和 `cd backend; go test ./...` 通过。

## 页面元素自动采集会话上下文绑定修复

- 变更内容：创建会话后保持 starting 状态；首次心跳必须携带非空 browser context 并原子绑定，后续心跳只能使用同一 context；同时校验创建最终 URL 和心跳 URL 均为绝对 http/https 地址。
- 影响范围：采集会话心跳服务与 PostgreSQL 条件更新，阻止跨浏览器上下文恢复和 `javascript:`、`file:`、相对地址写入会话。
- 验证结果：`cd backend; go test ./internal/service -run ElementCapture -count=1`、`cd backend; go test ./internal/repository -run ElementCapture -count=1` 和 `cd backend; go test ./...` 通过。

## 页面元素采集候选项审核与原子入库

- 变更内容：新增候选项接收、列表、审核更新和最多 200 项的批量保存；校验 SHA-256 指纹、定位器质量与唯一性、命名、冲突决策及会话状态，并在单个 PostgreSQL 事务中写入正式元素、版本、候选状态和审计日志。
- 影响范围：页面元素自动采集会话的候选审核和入库流程；重复元素按页面与指纹识别，候选达到 400 项时返回预警，超过 500 项拒绝接收。
- 验证结果：`cd backend; go test ./internal/service -run 'Candidate|BatchSave' -count=1`、`cd backend; go test ./internal/repository -run 'Candidate|BatchSave' -count=1` 与 `cd backend; go test ./...` 通过；SQL mock 已验证版本写入失败时事务回滚且不提交。

## 页面元素采集候选项审核并发加固

- 变更内容：候选业务 ID 恢复为随机字符串，并新增独立 `cursor_id` 支持安全的增量分页；批量事务锁定会话、页面和候选后重读候选状态，状态更新限定 `pending` 且核验受影响行数；同时补齐三组定位器与索引持久化，`ignore` 跳过质量门禁。
- 影响范围：候选分页兼容历史文本主键，批量审核在并发重放时拒绝已处理候选，正式元素与版本快照保留完整定位器组。
- 验证结果：`cd backend; go test ./internal/service -run 'Candidate|BatchSave' -count=1`、`cd backend; go test ./internal/repository -run 'Candidate|BatchSave' -count=1`、`cd backend; go test ./...` 和 `cd backend; go vet ./...` 通过。

## 页面元素采集审核事务复核修复

- 变更内容：外部候选 ID 统一为独立的 `cursor_id` 整数游标，业务主键继续保留字符串；批量保存改为事务中按请求游标锁定并重读完整候选字段、校验完整匹配和同页名称后重新执行质量、冲突、页面与状态门禁。
- 影响范围：候选审核 API 的 ID 语义、并发批量保存和候选更新；文本主键历史数据不再参与整数转换，更新名称可排除其自身重复目标但拒绝其他同名元素。
- 验证结果：`cd backend; go test ./internal/service -run 'Candidate|BatchSave' -count=1`、`cd backend; go test ./internal/repository -run 'Candidate|BatchSave' -count=1`、`cd backend; go test ./...` 和 `cd backend; go vet ./...` 通过；仓储外部契约测试验证真实仓储满足服务候选接口。

## 页面元素采集数据库并发不变量

- 变更内容：迁移前检测同页活动元素名称和非空指纹重复并返回明确错误；新增相应部分唯一索引及候选游标唯一索引。批量审核在页面锁后重读当前名称、指纹与重复目标，按最新状态复核冲突决策，并将唯一约束错误转换为候选领域冲突。
- 影响范围：所有页面元素普通写入和采集批量写入均受数据库层同页名称/指纹约束保护；历史重复数据不会被静默删除。
- 验证结果：`cd backend; go test ./cmd/api -run 'ElementCaptureMigration|PermissionSeeds' -count=1`、候选聚焦 service/repository 测试、`cd backend; go test ./...` 和 `cd backend; go vet ./...` 通过。

## 页面元素采集指纹冲突语义校准

- 变更内容：撤销同页指纹唯一索引并兼容已执行环境，保留显式 `create` 同指纹元素的审核决策；事务锁定会话全部 pending 候选后基于锁后指纹集合选择 update 目标或返回多目标歧义。
- 影响范围：候选重复 fingerprint 的 create/update/ignore 行为；数据库仍保证同页活动元素名称唯一，且仅该命名约束的唯一冲突会被转换为候选领域错误。
- 验证结果：迁移测试、候选聚焦 service/repository 测试、`cd backend; go test ./...`、`cd backend; go vet ./...` 与 `git diff --check` 通过。

## 页面元素采集审核目标确认协议

- 变更内容：批量审核项新增可选 `targetElementId`，多 fingerprint update 必须由用户显式确认目标；事务改为按会话锁定全部候选并按 cursor 排序，再筛选请求候选。更新候选名称时也仅将命名唯一索引冲突转换为领域错误。
- 影响范围：页面元素采集审核 API 的 update 决策与事务锁范围；单一锁后 fingerprint 目标仍可自动确定，多目标或非法目标会返回结构化审核问题。
- 验证结果：新增多目标确认服务测试、候选 service/repository 聚焦测试、`cd backend; go test ./...`、`cd backend; go vet ./...` 与 `git diff --check` 通过。

## 页面元素采集审核保证闭环

- 变更内容：批量预检与锁后复检统一计算有效更新目标，显式目标优先，当前单一同指纹目标可自动确定；跨页或失效目标、同批重复目标和更新名称自排除均使用该有效目标，`create`/`ignore` 携带目标会返回结构化 `CandidateIssue`。批量唯一约束文案收敛为仅名称冲突。
- 影响范围：候选接收容量与预警、列表可见性和游标、候选可编辑字段、全部质量门禁、create/update/ignore 决策，以及页面元素创建/更新/忽略、版本、审计、提交与回滚事务路径。
- 验证结果：候选 service/repository 表驱动聚焦测试通过；聚焦覆盖率中 `BatchSave` 85.7%、`validateBatchSave` 90.0%、仓储 `SaveCandidates` 74.3%；`cd backend; go test ./... -count=1` 与 `cd backend; go vet ./...` 通过。使用 `backend/.env.local` 的数据库配置在隔离临时 schema 中完成真实 PostgreSQL 旧结构升级和两轮幂等迁移验证，未输出连接凭据。

## 页面元素采集审核最终回归收口

- 变更内容：修正真实 PostgreSQL 迁移测试的资源清理顺序并显式报告 DROP/CLOSE 错误；旧版 fixture 现同时包含历史 fingerprint 唯一索引、cursor 自动唯一约束和命名唯一索引。新增预检/锁后目标集合变化、候选输入边界、空 actor、非 self 名称冲突、活动/软删除 fingerprint、锁后新单目标持久化及版本 V1/递增 SQL 回归。
- 影响范围：仅测试与验证保障，不改变页面元素采集生产行为；清理范围严格限定为 `element_capture_migration_[0-9]+` 测试 schema。
- 验证结果：清理 6 个遗留测试 schema 后确认剩余 0；旧 cleanup 可稳定复现单个 schema 泄漏，修复后真实 PostgreSQL 迁移测试通过且再次确认剩余 0。候选 service/repository 聚焦测试通过，最终覆盖率、全量测试、vet 与 diff-check 见 Task 3 报告。

## 页面元素采集平台与版本 API

- 变更内容：新增平台采集会话、候选审核、批量保存、元素版本查询和回滚路由；新增执行器心跳、候选接收、失败回调与命令查询入口。平台接口按查看、采集、管理和回滚权限控制，执行器回调同时传递执行器 ID 与一次性会话令牌；候选读写响应设置 `Cache-Control: no-store`，候选领域问题以统一 JSON 响应返回。
- 影响范围：页面元素自动采集的后端 HTTP 契约、权限目录和应用依赖注入；版本回滚会在单个事务内锁定正式元素、使用目标快照更新元素、生成 `current_version + 1` 的新版本（`source=rollback`）并写入审计日志，不会复用历史版本号。
- 验证结果：`cd backend; go test ./internal/controller -run ElementCapture -count=1`、`cd backend; go test ./... -count=1`、`cd backend; go vet ./...` 通过；`git diff --check` 将在提交前执行。

## 页面元素采集 API 授权与执行器边界加固

- 变更内容：采集会话、候选审核/保存和版本读取/回滚均在仓储查询与回滚事务中执行资源级授权；当前数据模型下普通用户只能访问页面或会话 `created_by` 为自己的资源，系统管理员按既有 roles 规则放行。执行器入口统一先读取 `X-Executor-ID` 与 `Authorization: Bearer <session-token>`，通过执行器 ID 和会话令牌哈希双校验后才解析业务载荷；创建、模式切换和停止会写入可消费的进程内会话命令队列。
- 影响范围：页面元素采集的后端鉴权、错误映射、候选缓存控制、版本快照和测试契约。当前 `ui_assets` 不含 `project_id`/`product_id`，因此未引入虚假外键；页面将来引入项目归属后，应迁移为基于 `project_members` 的资源授权。
- 验证结果：新增显式 `limit=0`、坏令牌先于 JSON 解析、未知错误脱敏、真实 `router.RegisterRoutes` 13 路由/四权限/候选领域错误/执行器认证、页面类型、命令消费、页面不可见版本及回滚快照字段测试；`cd backend; go test ./internal/controller -run ElementCapture -count=1`、`cd backend; go test ./internal/repository ./internal/service -run 'ElementCapture|RollbackWritesNextVersion|ListVersionsRejectsInvisible|AuthorizesExecutor' -count=1`、`cd backend; go test ./... -count=1` 与 `cd backend; go vet ./...` 通过，`git diff --check` 待提交前复核。
