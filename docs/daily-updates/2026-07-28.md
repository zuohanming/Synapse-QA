# 2026-07-28 每日更新

## 建立每日更新策略

- 变更内容：新增项目级每日更新规则及日志模板，要求每次修改或新增功能时同步记录。
- 影响范围：项目协作流程、变更记录和功能交付标准。
- 验证结果：已检查规则文件、模板及当日日志均已创建，Markdown 结构完整。

## 系统管理权限底座

- 变更内容：补齐菜单及系统管理权限目录；登录与请求鉴权实时加载用户角色和权限；系统概览、用户、角色、系统配置及操作日志接口增加权限校验；前端导航按用户权限过滤。
- 影响范围：用户登录态、系统管理接口、角色权限数据、左侧菜单可见性。
- 验证结果：后端 `go test ./...` 全部通过；前端 39 项 Vitest 测试全部通过；`npm run build` 构建成功，仅保留既有的大分包体积提示；重启后管理员加载 23 项权限且系统用户接口返回 200，只读角色加载 8 项权限且同一接口返回 403，临时验证账号已清理。

## 用户管理与角色权限矩阵

- 变更内容：新增管理员创建用户、编辑角色与状态、一次性临时密码、密码重置、首次登录强制改密；角色管理增加权限目录、权限矩阵、复制角色、启停及权限保存；用户或角色权限变化后通过 Token 版本立即使旧会话失效，并保护最后一个启用的超级管理员。
- 影响范围：登录与改密流程、用户管理、角色管理、权限会话校验及系统管理前端界面。
- 验证结果：后端 `go test ./...` 全部通过；前端 39 项 Vitest 测试全部通过；`npm run build` 构建成功，仅保留既有的大分包体积提示；端到端验证确认临时密码强制改密、改密与重置后旧 Token 返回 401、角色权限自动补齐菜单权限；Playwright 验证用户页、创建弹窗和 3 组权限矩阵可见，控制台无错误。

## 本地后端自动启动配置

- 变更内容：新增被 Git 忽略的 `backend/.env.local` 本机数据库配置、可提交的 `.env.example` 示例，并让 `start-api.ps1` 自动读取本地环境文件。
- 影响范围：本地后端构建与重启流程；数据库密码不会进入版本控制。
- 验证结果：`git check-ignore backend/.env.local` 确认配置文件已忽略；后台执行启动脚本后 `/api/health` 返回 `ok`，无需再次手工输入密码。

## 前端界面与交互重构

- 变更内容：依据 Taste Skill 的 `redesign-existing-projects` 审计流程，统一前端冷灰蓝色彩、字体层级、面板圆角与阴影；优化侧边导航、顶栏、首页概览、数据卡片、按钮、表单、表格、模态框的悬停、按压和焦点反馈；新增键盘“跳到主要内容”入口，并完善移动端间距。
- 影响范围：前端应用壳层及通用视觉组件；未修改 API 调用、数据结构、路由语义、权限判断或其他前后端业务逻辑。
- 验证结果：前端 `npm test` 共 9 个测试文件、39 项测试全部通过；`npm run build` 构建成功，仅存在既有的单个 JS 分包超过 500 kB 提示；浏览器在 1440×900 和 390×844 视口完成视觉核验，均无横向溢出，移动端导航折叠和首页单列布局正常。

## 五主题与工作台布局升级

- 变更内容：依据 `ui-ux-pro-max` 方案补齐全局语义化页面标题、统一焦点与加载反馈；新增“蓝图网格、午夜作战、精密实验室、管理驾驶舱、工业信号”五套可即时切换主题；重排接口管理的筛选区、主操作区和按需出现的批量操作栏；将接口测试用例编辑器优化为自适应三栏工作台，并提升文字尺寸、按钮触达面积与状态可感知性。
- 影响范围：前端应用壳层、通用页面标题、数据表格与状态组件、外观设置、接口管理、接口测试用例编辑器及响应式样式；未修改 API 调用、数据结构、权限判断或前后端业务逻辑。
- 验证结果：前端 `npm test -- --run` 共 9 个测试文件、39 项测试全部通过；`npm run build` 构建成功，仅保留既有的 JS 分包超过 500 kB 提示；真实浏览器在 1440×1000 与 390×844 视口核验接口管理页面，文档宽度与视口一致、所有可见按钮高度不低于 36px、未选中数据时不显示批量操作栏。

## 系统参数与通知默认策略

- 变更内容：新增执行策略、安全策略、通知默认策略三组系统参数，支持并发修改保护、版本历史和回滚；执行参数已接入接口用例批量上限、默认/最大并发和执行器离线判定；新增连续登录失败锁定及管理员解锁；通知订阅支持系统默认和个人覆盖，系统告警、安全告警保持强制开启；系统管理菜单新增“系统参数”“通知配置”，并将角色入口统一为“角色与权限”。
- 影响范围：系统管理前后端、登录安全、接口测试用例调度、执行器可用性判断、通知偏好及数据库初始化迁移。
- 验证结果：后端 `go test ./...` 全部通过并新增系统参数边界测试；前端 9 个测试文件、39 项测试全部通过，`npm run build` 构建成功（仅保留既有的大分包提示）；后端已自动重启，健康检查返回 `ok`，数据库已确认三组默认参数及登录锁定字段完成初始化。

## 系统运行概览与操作审计

- 变更内容：系统管理新增运行概览，汇总账号、角色、执行器在线率、今日执行/失败及近 7 日 UI 与接口执行趋势，并展示最近管理动作；操作日志升级为按操作人、动作、目标、日期筛选和分页查询，支持按当前条件导出带 BOM 的 CSV；修正后端启动脚本配置优先级，使项目 `.env.local` 覆盖进程遗留变量。
- 影响范围：系统概览接口与页面、操作日志查询及导出、系统管理菜单、后端本地自动重启流程和产品需求文档。
- 验证结果：后端 `go test ./...` 全部通过；前端 9 个测试文件、39 项测试全部通过，`npm run build` 构建成功（仅保留既有的大分包提示）；后端使用本地配置重新构建并启动，健康检查返回 `ok`。

## 修复系统概览查询失败

- 变更内容：修正近 7 日执行趋势 SQL 将 PostgreSQL 关键字 `day` 用作列别名导致的语法错误，统一改为明确的 `run_date` 别名，并抽取查询文本增加回归测试。
- 影响范围：系统管理“系统概览”接口及近 7 日执行趋势展示。
- 验证结果：已先通过数据库命令稳定复现原 SQL 语法错误；新增回归测试修复前编译失败、修复后通过；修正后的 SQL 在当前 PostgreSQL 返回完整 7 日数据；后端 `go test ./...` 全部通过，自动重启后健康检查返回 `ok`。

## 移除通用页面标题

- 变更内容：按确认意见取消各业务页面顶部的通用标题与说明区域，页面位置继续由顶栏面包屑表达，业务内容直接从工作区顶部开始。
- 影响范围：所有调用通用 `PageHeader` 的前端页面；未修改页面内部编辑对象名称、数据加载或业务逻辑。
- 验证结果：前端 `npm test -- --run` 共 9 个测试文件、39 项测试全部通过；`npm run build` 构建成功，仅保留既有的 JS 分包超过 500 kB 提示。

## 100% 缩放与列表溢出规范

- 变更内容：统一桌面浏览器 100% 缩放下的应用高度链路，将纵向超出内容收敛到工作区、资源面板和表格容器滚动；通用数据表格采用固定列宽和固定布局，字段超出列宽时显示省略号，并在真实截断时通过鼠标悬停或键盘聚焦显示完整内容气泡。
- 影响范围：应用工作区、资源面板、通用数据表格、分页列表及历史手写表格的滚动与字段展示；未修改列表数据、分页请求或其他业务逻辑。
- 验证结果：前端 `npm test -- --run` 共 9 个测试文件、39 项测试全部通过；`npm run build` 构建成功，仅保留既有的 JS 分包超过 500 kB 提示；真实浏览器在 1440×900、设备缩放因子 1 下核验，文档尺寸与视口一致，工作区纵向滚动、表格横向滚动和固定列布局生效，检测到的 3 个截断字段均能显示完整内容气泡。

## 接口管理自适应列表设计

- 变更内容：新增接口管理搜索区与列表自适应设计规范，明确桌面端单行弹性筛选、列表按字段优先级分配宽度、取消横向滚动及溢出气泡的实现边界和验收标准。
- 影响范围：设计文档与后续接口管理前端优化方案；本次尚未修改运行时代码。
- 验证结果：已完成规范占位符、内部一致性、范围和歧义检查，等待使用者确认后进入实现计划。

## V1 产品基线与归档设计

- 变更内容：新增 V1 产品基线与归档设计，明确采用“权威基线 + V1 固化归档包”的文档结构、四级功能状态、API 自动化待验收口径、归档维护规则和验收标准。
- 影响范围：后续产品需求、产品架构、功能状态和版本归档的文档组织方式；本次未修改业务代码、接口或数据库。
- 验证结果：设计的文档结构、内容边界和归档规则已经逐段获得使用者确认；待完成文档自检并提交后由使用者审阅。

## V1 产品基线与归档实施计划

- 变更内容：新增 V1 产品基线与归档实施计划，逐文件拆解需求、架构、功能状态、兼容入口、归档快照和终验任务，并明确检查命令与提交边界。
- 影响范围：V1 产品资料归档的后续执行方式；本次未创建正式归档，也未修改业务代码。
- 验证结果：已核对计划完整覆盖设计中的权威基线、状态口径、兼容入口、冻结归档和验收要求；未发现实际占位内容，文件路径与前后任务依赖一致，`git diff --check` 通过。

## V1 归档隔离工作区

- 变更内容：将项目内 `.worktrees/` 目录加入 Git 忽略规则，用于隔离执行 V1 产品归档计划。
- 影响范围：Git 本地工作区管理；不影响产品运行、构建或部署。
- 验证结果：待确认 `.worktrees/` 已被 `git check-ignore` 正确识别后创建隔离分支。

## 接口管理自适应布局实施计划

- 变更内容：基于已确认设计新增 TDD 实施计划，拆分通用表格容器适配、接口管理专属列宽与单行搜索、CSS 和真实浏览器验收三个可独立验证任务。
- 影响范围：实施计划文档；本次尚未修改运行时代码。
- 验证结果：已对照设计规范完成需求覆盖、占位符、接口命名和任务边界自检。

## 接口管理单行搜索与自适应列表

- 变更内容：接口管理桌面端筛选区改为七段单行弹性网格，操作区底部对齐且不换行；适配表格取消最小宽度与横向溢出，并收紧单元格及操作链接间距；小于 900px 时筛选区降为单列。
- 影响范围：接口自动化的“接口管理”页面布局，以及该页面的 Playwright 浏览器验收。
- 验证结果：`npm run e2e -- e2e/api-interface-layout.spec.js` 通过（1 项）；`npm test -- --run` 通过（10 个文件、42 项）；`npm run build` 成功（保留既有单个 JS 分包超过 500 kB 的警告）。

## 接口管理布局最终审查修复

- 变更内容：将 `operations` 与 `actions`、`select` 统一识别为操作单元格，避免操作按钮被文本裁切或附加溢出气泡焦点；接口筛选区改用 inline-size 容器查询，并收紧列表横向内边距以同时满足 1440px 单行和窄内容区单列；为全选与行选择 checkbox 补充可访问名称。
- 影响范围：通用 `DataTable` 的特殊列识别、接口管理列表的响应式布局与选择控件可访问性，以及对应 Vitest/Playwright 回归测试；未修改筛选、分页、接口请求和行操作业务逻辑。
- 验证结果：新增 Vitest 与 E2E 均先按预期失败；修复后目标 Vitest 2 个文件 8 项、全量 Vitest 10 个文件 43 项、目标 E2E 2 项全部通过；`npm run build` 成功，仅保留既有单个 JS 分包超过 500 kB 的警告。
