# 2026-08-04 每日更新

## 表单标签定位器可靠性修复

- 变更内容：表单标签定位器的匹配数改为按实际 XPath 回写；XPath 限定在最近的表单项、字段组或 `fieldset` 内，避免密码文案跨区域多次匹配；已实测唯一的表单标签定位器评分调整为可入库等级。
- 影响范围：页面元素自动采集的表单控件定位质量判定。
- 验证结果：统一认证登录页真实浏览器完整拾取链路中，“账号名”和“密码”定位器均为唯一、评分 75；执行器全量测试通过（134 passed，2 skipped）。

## 输入框候选被安全复核丢弃修复

- 变更内容：统一拾取脚本和 DOM 安全复核脚本的属性白名单，避免 `autocomplete` 导致两次提取结果不一致而丢弃账号、密码输入框候选。
- 影响范围：带 `autocomplete` 属性的页面元素自动采集。
- 验证结果：统一认证登录页真实浏览器点击账号、密码输入框均收到候选；执行器定向测试通过（63 passed，1 skipped）。

## 统一认证输入框采集与候选删除

- 变更内容：表单上下文采集兼容 Element UI 表单项中的 `.name` 文本标签，使统一认证登录页的账号名、密码输入框可生成可验证的上下文定位器；候选审核支持删除待处理候选，并同步更新会话候选数量。
- 影响范围：页面元素自动采集、候选审核前端与候选管理 API。
- 验证结果：后端 `go test ./... -count=1` 通过；前端候选组件及服务测试 27 项通过、构建通过；执行器 `python -m pytest -q` 通过（133 passed，2 skipped）。

## 表单控件上下文定位

- 变更内容：采集器读取最近表单项标签，并增加 `name`、`aria-label`、`placeholder`、`autocomplete` 的 CSS 定位候选；标签上下文会生成 XPath 候选并由执行器实测匹配数量，同时作为未命名控件的候选名称。
- 影响范围：页面元素自动采集，尤其是账号、密码等复用同一 UI 组件的表单控件。
- 验证结果：`python -m pytest -q` 通过（133 passed，2 skipped）。

## 浏览器手动关闭采集同步

- 变更内容：执行器监听 Playwright 浏览器断开事件；用户直接关闭采集浏览器窗口时，自动清理本地采集会话并向平台回报 `browser_closed`，不再遗留活动会话。
- 影响范围：有头页面元素采集的执行器状态同步与再次启动流程。
- 验证结果：`python -m pytest tests/test_capture_session_manager.py -q` 通过（32 passed，1 skipped）。

## 页面元素自动采集工作台集成

- 变更内容：在页面元素配置中接入自动采集启动弹窗和候选审核抽屉；采集保存后自动刷新元素列表。新增元素采集元数据展示、版本历史列表和生成新版本式回滚确认。
- 影响范围：界面自动化页面元素工作台、自动采集前端服务与样式。
- 验证结果：`npm test -- --run src/pages/UIAutomationPage.test.js src/components/ElementCaptureLauncher.test.js src/components/ElementCaptureDrawer.test.js src/services/elementCaptureService.test.js` 通过（53 项）；`npm test -- --run` 通过（74 项）；`npm run build` 通过，保留既有大于 500 kB chunk 警告。

## 自动采集清理与接口文档

- 变更内容：采集调度器每分钟删除到期候选并清空终态会话令牌摘要；API 使用可取消应用上下文，退出时停止后台调度并优雅关闭 HTTP 服务。新增平台/执行器接口、权限、状态机、错误码、容量和安全限制文档，并补充 PRD。
- 影响范围：后端采集仓储、调度器、API 生命周期和产品文档。
- 验证结果：清理定向测试、`go test ./... -count=1` 与 `go vet ./...` 通过。真实 Chromium E2E 未宣称通过：可信用户事件、DNS 连接绑定、完整 iframe 截图遮罩及执行器后台线程退出仍是发布阻断项。

## 验收回归修复

- 变更内容：修正 PostgreSQL 候选幂等测试夹具，补齐仓储查询所需的页面元素名称字段。
- 影响范围：仅测试数据库临时表结构。
- 验证结果：修复后重新执行后端全量测试、`go vet`、执行器全量测试和前端全量测试/构建。

## 有头采集执行器列表修复

- 变更内容：启动采集弹窗兼容执行器列表接口直接返回数组的契约，避免将在线 UI 执行器错误识别为空列表。
- 影响范围：页面元素自动采集启动弹窗。
- 验证结果：前端组件测试、全量测试和构建待本次修改后执行。

## 有头采集执行器运行版本修复

- 变更内容：执行器平台地址和本地回调地址改为与 Token 一致地支持 Windows 用户环境读取，便于新进程加载正确的有头采集配置；旧执行器不支持采集命令轮询，改由新版本独立端口服务接管。
- 影响范围：执行器启动与页面元素有头采集命令消费。
- 验证结果：待新版本执行器启动、命令领取及浏览器启动链路验证。

- 补充修复：执行器服务入口不再写死 8090，改为使用 `EXECUTOR_ENDPOINT` 中配置的主机和端口，允许新版有头采集执行器与旧服务隔离运行。
- 补充修复：内网采集主机白名单也支持 Windows 用户环境读取；已仅放行当前测试域名，避免默认 SSRF/DNS 防护拦截已授权的内网测试站点。

## 关闭采集器同步停止

- 变更内容：关闭候选审核抽屉时，活动会话会先向平台提交停止命令，等待停止请求成功后才关闭界面；执行器命令轮询随即关闭浏览器，下一次有头采集会创建新会话并重新启动浏览器。
- 影响范围：页面元素自动采集抽屉关闭行为。
- 验证结果：前端组件和全量测试待本次修改后执行。
