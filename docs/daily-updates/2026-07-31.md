# 2026-07-31 每日更新

## AI 设置功能 - LLM Provider 配置管理

- 变更内容：打通 AI 设置的完整前后端链路。后端补齐 AI 依赖注入和路由注册（GET /api/ai/config、POST /api/ai/providers、POST /api/ai/test-connection）；前端新增 aiService.js API 服务层，将 AI 设置页从静态占位替换为实际功能的 Provider 管理面板，支持启用/禁用、API Key 配置、Base URL 自定义、连接测试、场景偏好查看和安全策略展示。
- 影响范围：后端 main.go（DI 装配）、router.go（AI 路由注册）、前端 AIPage.js（新 AISettings 组件）、aiService.js（新建）、theme-blue.css（新增 AI 设置样式 + 补充 --card-bg/--border-color 变量）。
- 验证结果：后端 go build 编译通过；前端 vite build 构建通过。功能验证待启动服务后进行端到端测试。

## AI 配置按用户隔离

- 变更内容：将 AI 配置从共享改为按用户隔离存储。存储 key 从 `ai.config` 改为 `ai.config.{userId}`，移除全局内存缓存，Controller 从 JWT claims 提取 userId 传给 Service 层。
- 影响范围：ai_repository.go、ai_service.go、ai_controller.go。
- 验证结果：后端 go build 通过，API 测试返回用户专属配置。

## AI 助手功能实施

- 变更内容：实现 AI 助手（Copilot）流式对话功能。后端新增 POST /api/ai/chat SSE 流式端点，支持 OpenAI 兼容的 Chat Completions Streaming API（覆盖 OpenAI/DeepSeek/OpenRouter）；前端将占位页替换为完整聊天界面，支持多轮对话、流式输出、打字动画、错误提示、清空对话。
- 影响范围：ai_config.go（新增 AIChatRequest/AIChatMessage 模型）、ai_service.go（新增 StreamChat + ParseOpenAIStream）、ai_controller.go（新增 Chat handler）、router.go（注册 /api/ai/chat）、aiService.js（新增 chatStream 方法）、AIPage.js（新 AICopilot 组件）、theme-blue.css（新增聊天界面全套样式）。
- 验证结果：后端 go build、前端 vite build 均通过，服务重启后 /api/ai/chat 路由正常注册。

## AI 设置重构为模型为中心架构

- 变更内容：将 AI 设置从"预设 Provider + 配置 Key"模式重构为"用户新建模型"模式。后端数据模型从 `providers map[string]AIProviderConfig` 改为 `models []AIModel`（每个模型含 ID、Provider、Name、Label、APIKey、BaseURL、Enabled），新增 SaveModel（upsert）和 DeleteModel 接口替换旧 SaveProvider 路由。前端 AIPage.js AISettings 组件完全重写为"新建模型"表单 + 模型卡片列表 + 场景偏好下拉选择模式。
- 影响范围：后端 model/ai_config.go、service/ai_service.go、controller/ai_controller.go、router/router.go；前端 AIPage.js（AISettings 组件重写）、aiService.js（新增 saveModel/deleteModel）、theme-blue.css（替换 provider 卡片样式为模型卡片样式）。
- 验证结果：后端 go build 编译通过，API 端到端验证通过（GET /api/ai/config、POST /api/ai/models、DELETE /api/ai/models/:id 均返回正确）。

## AI 助手聊天记录持久化

- 变更内容：AI 助手聊天记录支持 localStorage 持久化，刷新页面后保留最近 50 条对话，清空对话时同步清除存储。
- 影响范围：前端 AIPage.js（AICopilot 组件：lazy init 加载历史、useEffect 自动保存、clearChat 清除 localStorage）。
- 验证结果：vite build 编译通过。

## AI 助手接入全部业务模块

- 变更内容：将 AI 助手 Tool Calling 从接口管理扩展到全部核心模块，新增 7 个工具：list_products、list_modules（产品/模块）；search_test_cases、get_test_case、create_test_case（测试用例）；list_execution_runs、get_execution_run（执行任务）；list_test_objects（测试环境）。总计 11 个工具覆盖系统所有核心业务。create_interface 和 create_test_case 支持自动产品发现。提取通用工具函数（toMapSlice、parseIntFromJSON、autoDiscoverProduct、truncate）。
- 影响范围：后端 ai_tools.go（新增 TestCaseService、ExecutionService、AutomationService 依赖），main.go（更新注入）；前端无需改动（UI 已适配通用工具事件）。
- 验证结果：后端 go build 通过，5 个核心工具端到端验证通过 —— list_products 返回 5 个产品、list_modules 返回 3 个模块、search_test_cases 返回 2 个用例、list_execution_runs 返回 19 条记录中最近 5 条。

## AI 助手全模块增删改查覆盖

- 变更内容：将 AI Tool Calling 从 11 个工具扩展到 22 个，覆盖全部 5 个核心模块的完整 CRUD：
  - 接口管理（5 个）：search_interfaces、create_interface、get_interface、delete_interface、update_interface
  - 产品/模块（8 个）：list_products、list_modules、create_product、update_product、delete_product、create_module、update_module、delete_module
  - 测试用例（4 个）：search_test_cases、get_test_case、create_test_case、delete_test_case
  - 执行任务（3 个）：list_execution_runs、get_execution_run、cancel_execution_run
  - 测试环境（4 个）：list_test_objects、create_test_object、update_test_object、delete_test_object
  **修复必填字段问题**：updateProduct 通过 ListProducts(ID) 自动获取已有 ProjectID/UiType/ApiType；updateModule 工具定义新增 productId 为必填参数；updateTestObject 通过 ListTestObjects(ID) 自动获取已有 ProductID/Owner 等字段；createTestObject Owner 默认设为"AI助手"。
- 影响范围：backend/internal/service/ai_tools.go（新增 11 个工具实现 + 修复 4 个更新操作）、backend/cmd/api/main.go（不变，DI 已在之前阶段完成）。
- 验证结果：后端 go build 编译通过，前端 vite build 编译通过。端到端测试（LLM Tool Calling）需配合已配置的 AI 模型。
