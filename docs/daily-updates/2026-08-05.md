# 2026-08-05 每日更新

## 页面元素提取框架兼容

- 变更内容：兼容 Element UI、Ant Design、Naive UI 的表单项和标签结构；唯一 placeholder 定位器按可靠属性评分；点击下拉选项展示文本时自动提升至输入框或选择组件进行采集。
- 影响范围：页面元素采集候选的标签识别、定位器生成、质量评分和可保存状态。
- 验证结果：执行 `D:\pyhon3.11.8\python.exe -m pytest -q tests\test_locator_generator.py tests\test_element_picker.py tests\test_capture_session_manager.py tests\test_capture_security.py`，123 passed、2 skipped。

## 同站点跳转后的采集工具恢复

- 变更内容：拾取器对当前会话内的同站点子域 frame 保持信任；页面从业务子域跳转到登录子域后，自动释放旧页面控制器并重新注入拾取/操作工具栏。
- 影响范围：从 `upms-ui-qa1.oojoyoo.com` 跳转到 `signinunifly-qa1.oojoyoo.com` 的有头页面元素采集会话。
- 验证结果：执行 `D:\pyhon3.11.8\python.exe -m pytest -q tests\test_element_picker.py tests\test_capture_session_manager.py tests\test_capture_security.py`，85 passed、2 skipped。

## 有头采集同站点请求放行

- 变更内容：页面元素采集会话在启动时已完成目标页面 URL 和网络安全校验；当前页面站点及其同站点资源请求直接放行，不再被重复 DNS 校验中止。
- 影响范围：从 `upms-ui-qa1.oojoyoo.com` 跳转至 `signinunifly-qa1.oojoyoo.com` 等同站点跨子域登录流程、页面加载与接口调用；跨站点请求和顶层跳转仍受拦截。
- 验证结果：执行 `D:\pyhon3.11.8\python.exe -m pytest -q tests\test_capture_session_manager.py tests\test_capture_security.py`，55 passed、1 skipped。

## 项目产品基础信息优化

- 变更内容：产品模型与管理界面增加编码、负责人、状态；旧数据启动迁移时自动生成编码并默认启用，编辑时编码不可修改。
- 影响范围：测试配置中的项目产品列表、新增与编辑，以及所有读取产品信息的下游页面。
- 验证结果：`go test ./internal/service/... ./internal/repository/...` 通过；前端 `npm run build` 通过。

## 产品关联资产统计与删除保护

- 变更内容：产品详情展示模块、环境、页面、接口、UI/API 用例统计；删除前校验模块、环境及 UI 用例关联关系。
- 影响范围：项目产品详情和删除操作。
- 验证结果：`go test ./...` 通过；前端 `npm run build` 通过。

## 复制产品

- 变更内容：新增复制产品接口及列表操作入口，复制基本信息和两级模块，新产品默认启用。
- 影响范围：测试配置中的项目产品维护。
- 验证结果：`go test ./...` 与前端 `npm run build` 通过。

## 停用产品约束

- 变更内容：停用产品不再允许新建 UI 测试用例与测试环境；既有资产不受影响。
- 影响范围：测试用例和测试对象创建。
- 验证结果：`go test ./...` 通过。

## 产品分组视图

- 变更内容：产品列表支持项目分组卡片视图和表格视图切换，卡片可直接进入产品模块与资产统计详情。
- 影响范围：测试配置 / 项目产品。
- 验证结果：前端 `npm run build` 通过。

## 产品与模块界面优化

- 变更内容：产品卡片改为项目分组工作台样式，突出编码、状态和负责人；模块详情的六类关联资产改为紧凑响应式数据摘要。
- 影响范围：测试配置 / 项目产品与产品模块详情。
- 验证结果：前端 `npm run build` 通过。

## 项目产品工作台视觉重构

- 变更内容：基于高密度 SaaS 工作台设计系统，重构项目分组、产品卡片与资产统计的视觉层级，并补充键盘焦点和响应式布局。
- 影响范围：测试配置 / 项目产品与产品模块详情。
- 验证结果：前端 `npm run build` 通过。

## 产品卡片密度调整

- 变更内容：采用数据密集型后台列表风格，产品卡片改为低圆角、弱装饰、状态点与分隔线布局。
- 影响范围：测试配置 / 项目产品卡片视图。
- 验证结果：前端 `npm run build` 通过。

## 产品模块配置工作台

- 变更内容：模块导航支持一级、二级树形下钻筛选，重构导航面板、筛选标签和模块结果区域的视觉层级。
- 影响范围：测试配置 / 项目产品 / 产品模块配置。
- 验证结果：前端 `npm run build` 通过。

## 模块层级下拉选择

- 变更内容：新增/编辑模块的一级、二级模块改为下拉选择；未选一级模块时禁用二级模块选择。
- 影响范围：产品模块新增与编辑弹窗。
- 验证结果：前端 `npm run build` 通过。

## 一级模块默认归类

- 变更内容：新增/编辑模块未选择一级、二级模块时，自动使用模块名称作为一级模块，避免产生未分组模块。
- 影响范围：产品模块保存与树形导航展示。
- 验证结果：前端 `npm run build` 通过。

## 统一模块层级模型

- 变更内容：模块配置界面取消一级、二级、三级模块概念，仅保留模块名称及非必选的上级模块选择；兼容使用既有层级字段保存上级模块。
- 影响范围：产品模块新增、编辑、筛选、树形导航与列表展示。
- 验证结果：前端 `npm run build` 通过。

## 已处理候选项的状态同步

- 变更内容：服务端发现候选项已处理时立即结束该候选校验，避免附带“页面内元素名称重复”；前端收到该状态冲突后自动移除过期候选和选择状态。
- 影响范围：页面元素采集候选项的批量保存、重复提交与多窗口并行审核场景。
- 验证结果：后端执行 `go test ./internal/service/...` 通过；前端执行 `npm test -- --run src/components/ElementCaptureDrawer.test.js`（21 passed）及 `npm run build` 通过。

## 页面刷新后采集工具栏消失

- 变更内容：框架发生导航时强制重新注入拾取器，不再仅以 URL 判断控制器是否可复用；旧控制器会先被释放，页面刷新后可继续切换“拾取 / 操作”。
- 影响范围：有头页面元素采集中的登录、页面刷新与同 URL 重载场景。
- 验证结果：执行 `D:\pyhon3.11.8\python.exe -m pytest -q tests\test_element_picker.py tests\test_capture_session_manager.py tests\test_capture_security.py`，82 passed、2 skipped。

## 登录网关请求被错误拦截

- 变更内容：正确调用 Playwright 请求对象的 `is_navigation_request()` 方法，避免将所有请求对象误判为顶层导航；同站点网关的 XHR 请求可使用页面地址会话级授权继续访问。
- 影响范围：有头采集浏览器中页面发起的同站点跨子域接口请求。
- 验证结果：执行 `D:\pyhon3.11.8\python.exe -m pytest -q tests\test_capture_session_manager.py tests\test_capture_security.py tests\test_element_picker.py`，82 passed、2 skipped。

## 同源路由切换后的操作模式

- 变更内容：同源页面路由变化导致拾取器重新注入时，先执行旧控制器的 `dispose`，解除旧的鼠标与键盘监听，再释放其句柄，避免残留拾取模式拦截点击。
- 影响范围：有头页面元素采集在 SPA 路由跳转后的“操作 / 拾取”模式切换。
- 验证结果：执行 `D:\pyhon3.11.8\python.exe -m pytest -q tests\test_element_picker.py tests\test_capture_session_manager.py tests\test_capture_security.py`，81 passed、2 skipped。

## 页面地址会话级网络授权

- 变更内容：页面元素有头采集不再使用执行器静态私网域名白名单；采集页面地址及其同站点子域名仅在当前会话中临时获得私网请求授权，其他站点和 IP 地址仍会被拦截。
- 影响范围：需要访问同站点网关的页面元素采集浏览器，例如统一认证页调用同站点登录网关。
- 验证结果：执行 `D:\pyhon3.11.8\python.exe -m pytest -q tests\test_capture_security.py tests\test_capture_session_manager.py`，52 passed、1 skipped。

## 操作模式不应拾取元素

- 变更内容：拾取脚本按当前鼠标事件的 `altKey` 判断临时反转模式，并在模式切换时清空残留修饰键状态，避免操作模式被误反转为拾取模式。
- 影响范围：有头页面元素采集的拾取/操作模式切换。
- 验证结果：统一认证登录页真实浏览器验证：操作模式点击输入框采集数为 0，切回拾取模式后采集数为 1；执行器全量测试通过（136 passed，2 skipped）。

## UI 框架按钮定位兼容

- 变更内容：原生 `button` 自动生成 role/text 语义定位；带下划线命名空间的稳定业务 CSS 类自动作为框架兼容候选，避免 Element UI 等框架的通用类导致定位评分过低。
- 影响范围：页面元素自动采集中的按钮定位与可靠性评分。
- 验证结果：统一认证登录页真实浏览器完整拾取中，“登录”按钮生成唯一 CSS 定位器评分 78、role/text 定位器评分 73；执行器全量测试通过（135 passed，2 skipped）。
