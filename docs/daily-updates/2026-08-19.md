# 2026-08-19 每日更新

## AI 工具与执行统计安全收口

- 变更内容：AI 对话链路传递完整 Claims，按权限过滤执行工具，并让列表、详情、取消执行统一走项目级 scoped 服务；执行统计改为按用户可见范围在 PostgreSQL 内聚合，保留 14 天趋势和原有变化率口径，同时补充权限、越权调用和 SQL 聚合测试。
- 影响范围：`backend/internal/controller`、`backend/internal/service`、`backend/internal/repository` 及相关测试；普通用户仅可访问所属项目执行记录，管理员可查看历史 `project_id IS NULL` 批次。
- 验证结果：已通过 `$env:GOMAXPROCS='2'; go test ./...`、`$env:GOMAXPROCS='2'; go vet ./...`、`$env:GOMAXPROCS='2'; go build ./cmd/api`；重启后端后，真实 PostgreSQL/API 验证执行统计总批次为 19、趋势固定返回 14 天，未登录请求返回 401；Dashboard 返回 `state=ok`、执行总数 21、最近执行 5 条、失败事项 3 条，项目筛选回显正确，不存在项目返回 404。独立安全审查未发现 Blocker 或 High 风险；`git diff --check` 通过，仅有既有换行符提示。
