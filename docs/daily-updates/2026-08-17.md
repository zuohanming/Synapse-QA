# 2026-08-17 每日更新

## 修复左侧菜单重复的「AI 智能」入口

- 变更内容：左侧菜单出现两个「AI 智能」入口，原因是 `frontend/src/config/appConfig.js` 的 `menuData` 中「AI 智能」菜单组被重复声明了两份（标题与子项完全一致，渲染时产生重复 key 与重复菜单组）。删除位于「执行中心」与「系统管理」之间的重复项，仅保留「数据工厂」与「测试配置」之间的一个「AI 智能」入口，子菜单（AI 助手、智能断言、用例生成、失败分析、AI 设置）保持不变。
- 影响范围：`frontend/src/config/appConfig.js`（`menuData` 删除重复的「AI 智能」菜单组）。
- 验证结果：`npm run build` 成功，仅保留既有 chunk size 提示（>500 kB，与本次改动无关）；前端 Vite 开发服务器 HMR 热更新生效，左侧菜单仅显示一个「AI 智能」。

## 修复执行器连接异常后无法自愈的问题

- 变更内容：执行器进程虽存活、`/health` 正常，但心跳循环因 Token 鉴权失败（401）触发 `stop_event` 后永久停止，之后即便 Token 被修正也无法自愈，后端据此判定执行器离线/连接异常。修改 `executor/app/services/heartbeat_client.py` 的 `authenticate()`：连接成功后先将验证通过的 Token 写回内存 `settings.executor_shared_token`，再调用 `start()` 重启心跳循环——`start()` 会清除停止标志并在循环已停止时重新拉起 `_loop`，循环仍在运行时自动跳过重复启动。这样 GUI 登录成功（或 Token 失效后重新登录）即可自动恢复心跳，无需手动重启执行器进程。
- 影响范围：`executor/app/services/heartbeat_client.py`（`authenticate()` 成功分支）、`executor/tests/test_heartbeat_client.py`（新增成功场景测试）。
- 验证结果：`pytest tests/test_heartbeat_client.py` 6 项通过（含新增的 `test_authenticate_success_updates_token_and_restarts_heartbeat`）；执行器全量 `pytest` 为 102 passed、59 skipped、1 failed，唯一失败 `test_fastapi_lifecycle_starts_one_capture_poller_and_health_is_non_sensitive` 因环境缺少 `httpx` 模块（`No module named 'httpx'`）而失败，为预先存在的环境问题、与本次改动无关；重启执行器加载新代码后心跳恢复，数据库 `executors.status=online` 且 `last_heartbeat_at` 每 10 秒持续更新。

## 修复打包版执行器调试页面步骤报「playwright install」

- 变更内容：桌面打包版执行器（PyInstaller 生成的 `synapse-executor.exe`）在「调试页面步骤」启动浏览器时，报 `Looks like Playwright was just installed or updated. Please run playwright install`。根因是 Playwright 在 frozen（PyInstaller）环境下会把 `PLAYWRIGHT_BROWSERS_PATH` 默认置为 `"0"`，从而到驱动目录下的 `.local-browsers` 查找浏览器，而浏览器实际安装在用户级 `ms-playwright` 缓存（`%LOCALAPPDATA%\ms-playwright`）中。修复分两层：① 在 `executor/app/core/config.py` 顶部新增 `_ensure_playwright_browsers_path()`，frozen 且未显式指定路径时自动把 `PLAYWRIGHT_BROWSERS_PATH` 指向用户级 `ms-playwright` 缓存，使未来重新打包的 exe 自包含；② 同时写入用户环境变量 `PLAYWRIGHT_BROWSERS_PATH` 并重启当前 exe，让已打包的 exe 立即生效。
- 影响范围：`executor/app/core/config.py`（新增 frozen 下的浏览器路径修正）；运行环境新增用户环境变量 `PLAYWRIGHT_BROWSERS_PATH`（`C:\Users\Administrator\AppData\Local\ms-playwright`）。
- 验证结果：用 venv 复现确认 `PLAYWRIGHT_BROWSERS_PATH=0` 时 `chromium.launch()` 报 `Executable doesn't exist ... Looks like Playwright was just installed or updated`（与用户所见一致），改为指向 `ms-playwright` 后 `launch` 成功；`config.py` 非 frozen 时不设置该变量、frozen 时正确设为 `ms-playwright` 路径；`pytest tests/test_heartbeat_client.py` 6 项通过；重启 `synapse-executor.exe` 后窗口正常启动。

## 新增性能测试业务模块

- 变更内容：新增「性能测试」一级菜单模块（二级菜单：压测方案、测试报告）。压测方案支持按项目/产品维护目标接口、请求方法与请求头/请求体，并配置 K6 风格负载（固定并发 vus+duration / 爬坡 stages）与阈值断言 thresholds；提供筛选、新增、编辑、详情、删除、触发执行。测试报告支持方案关联、状态、总请求数、平均耗时、P95、错误率、RPS 等 K6 指标字段与详情指标卡片展示。底层压测引擎选定 Grafana k6（后续通过执行器以「k6 二进制 + 子进程 + handleSummary 写 JSON」方式接入真实压测），本次触发执行先落一条待执行记录占位。
- 影响范围：后端新增 `backend/internal/{model,repository,service,controller}/performance*.go` 分层，并接入 `router.go`（路由注册）、`main.go`（装配）、`bootstrap.go`（建表 perf_test_plans、perf_test_runs）、`api_migration.go`（权限码 `menu.perf_test.read`）；前端新增 `PerformancePage.js`/`PerfPlansPage.js`/`PerfRunsPage.js`/`performanceService.js`，并接入 `appConfig.js`（菜单）、`Layout.js`（图标与权限）、`routes/index.js`（路由）、`global.css`（性能测试样式）。
- 验证结果：后端 `go build ./...`、`go vet ./...`、`go test ./...` 全部通过（新增 service 12 个、controller 8 个测试函数通过）；前端 `npm run build` 通过（仅既有 chunk size 提示）、`npm run test` 85 个用例全部通过。

## 新增性能测试真实需求 SPEC

- 变更内容：新增 `docs/performance-testing-spec.md`，梳理性能测试真实需求（v2）。定义 P0（真实 K6 执行闭环、脚本生成、结果回填、阈值判定、执行生命周期）/ P1（实时监控、多接口混合场景、请求构造增强、报告图表）/ P2（基线对比、报告导出、分布式压测）三级能力清单，给出数据模型增量、执行链路（执行器 perf runner + 后端调度 + 结果回填）、实时监控、报告、基线对比的技术方案与分阶段验收标准。底层引擎沿用 Grafana k6，集成方式为「独立二进制 + 子进程 + handleSummary 写 JSON」。
- 影响范围：新增文档 `docs/performance-testing-spec.md`；不涉及代码变更。
- 验证结果：纯文档梳理，待评审确认后再进入实施；无需构建/测试验证。

## 以 6 大测试场景重写性能测试需求 SPEC

- 变更内容：按 6 大测试场景重构 `docs/performance-testing-spec.md`——基准测试（baseline）、梯度压力（ramp）、峰值负载（peak）、极限压力（stress）、稳定性/耐久（soak）、真实场景混合（mixed）。以「场景类型 scenario_type」为一等分类，给出每个场景的作用、负载方式、K6 executor 映射、关键参数（统一由 `load_config jsonb` 承载）与核心观察指标；数据模型由 `load_mode` 迁移为 `scenario_type + load_config`；执行链路按场景类型渲染 k6 脚本；报告按场景定义指标关注点；验收标准分 P0（6 场景建模 + 5 个单接口场景真实执行）/ P1（mixed + 实时监控）/ P2（基线对比 + 导出 + 分布式）。
- 影响范围：`docs/performance-testing-spec.md`（重写）；不涉及代码变更。
- 验证结果：纯文档设计，待评审确认后进入实施；无需构建/测试验证。

## 按评审意见全面修订性能测试 SPEC（v2.1）

- 变更内容：依据 @oracle 独立评审结论修订 `docs/performance-testing-spec.md`。修正调度模型（明确「机制复用、数据载体不复用」：复用 pickExecutor/submitToExecutor/cancelExecutorTask/回调鉴权原语，perf 走独立 `perf_test_runs` 表 + 独立回调端点 `/api/perf/tasks/:taskId/callback`，不再复用通用 execution_runs 调度）；补超时/取消/并发设计（perf runner 按 load_config duration 动态超时、取消强杀 k6 子进程并落盘部分结果、单执行器 perf 并发上限 1）；补 k6 检测链路（heartbeat `_checks` 加 `shutil.which("k6")` 探测、调度时校验）；补数据模型迁移细节（删旧列 load_mode/vus/duration/stages、model/repository 改动清单、p99 仅存 summary）；补安全与权限（目标安全、凭证保护、脚本转义、读/管理/执行三档权限）；补完成/失败通知；砍掉分布式压测（降为长期愿景）。状态升为「已评审」。
- 影响范围：`docs/performance-testing-spec.md`（全面修订）；不涉及代码变更。
- 验证结果：纯文档修订，待进入 P0 实施时再做构建/测试验证。

## 按补充建议优化性能测试 SPEC（v2.2）

- 变更内容：融入补充评审建议优化 `docs/performance-testing-spec.md`。补执行快照（`plan_snapshot`/`config_hash`/`executor_id`/`k6_version`/`environment`/`requested_at`/`script_snapshot`，改方案后旧报告仍还原旧配置）；状态机拆终态（completed / threshold_failed / execution_failed / timed_out / canceled）与中间态（pending→queued→running→stopping），条件更新避免竞态；补可靠性（idempotency_key 幂等、回调幂等/乱序、执行器掉线判失败、后端重启恢复、回调大小上限、临时文件清理）；实时曲线改有限留存（1~5s 降采样存 `series`，每项 ≤1000~3000 点，解决趋势图数据来源）；明确资源监控边界（k6 仅请求侧，泄漏需 Prometheus/Grafana）。前端补信息架构三页面（方案/执行监控/报告）、方案编辑页四区块（场景卡片 + Params/Headers/Body Tab + 负载预览图 + 结构化阈值）、执行确认面板与生产环境二次确认、运行中页面与 SSE 断线处理、报告四屏层级（结论/阈值/趋势分布/诊断信息）、前端验收标准。
- 影响范围：`docs/performance-testing-spec.md`（全面修订至 v2.2）；不涉及代码变更。
- 验证结果：纯文档优化，待进入 P0 实施时再做构建/测试验证。

## 按第二轮 P0 阻断问题评审修订性能测试 SPEC（v2.3）

- 变更内容：采纳第二轮评审 11 项后端问题 + 前端补充，修订 `docs/performance-testing-spec.md`。修正 `executor_id` 类型为 `text references executors(executor_id)`（原误写 bigint）；补 `task_id text unique`、`dispatched_at`、`idempotency_key`（`unique(triggered_by, idempotency_key)`）、部分唯一索引 `uq_perf_plan_active`（同方案活动任务唯一）；状态机固化为含 dispatched 的五中间态 + 五终态 + 合法转移表（非法转移 409）；固化掉线/重启恢复（running/stopping 永不自动重投，用 `expected_finish_at = started_at + planned_duration + timeout_buffer` 结合执行器查询恢复）；解决取消部分结果矛盾（P0 纳入最低限度 NDJSON 本地采样兜底，不做实时前端曲线；区分优雅停止/强制终止）；阈值改为平台结构化模型（metric/aggregation/operator/value/unit/abortOnFail/delayAbortEval，执行器再生成 k6 thresholds）；快照脱敏规则（存变量引用/未解析模板/script_hash，不存运行时脚本）；environment 明确 P0 枚举仅风险标签、P2 才多环境切换；mixed weight 定义为每次迭代随机选择概率（总和=1）；冒烟测试请求明确走执行器；迁移改为分版本发布顺序。前端补 URL 路由（/performance/plans/:id/edit、/performance/runs/:id）、P0/P1 页面能力分离、长任务离开提示。状态标「评审中」。
- 影响范围：`docs/performance-testing-spec.md`（修订至 v2.3）；不涉及代码变更。
- 验证结果：纯文档修订，待进入 P0 实施时再做构建/测试验证。

## 收口性能测试 SPEC 第三轮评审问题（v2.4）

- 变更内容：直接修订 `docs/performance-testing-spec.md` 并完成 P0 评审收口。将 `task_id` 从回调凭据中分离，新增 `callback_token_hash`，回调改用 Bearer Token 鉴权且终态后失效；幂等 key 改为 UUID 永久唯一，消除「24 小时可复用」与永久唯一索引的冲突；状态机新增 `dispatching`，明确先持久化 task_id/凭据哈希/executor_id，再幂等提交执行器，并允许快速启动回调从 dispatching 直接转 running；`script_hash` 改为执行器渲染脱敏脚本后上报；P0 强制 `abortOnFail=false`，自动停止开关延后至 P1；补 `failure_stage`/`diagnostic_output`/`needs_attention`，明确 P0 仅留最后 32 KB 脱敏诊断输出；冒烟请求明确为 `type:perf + mode:smoke` 的单请求任务。文档版本升至 v2.4，状态改为「已评审」。
- 影响范围：`docs/performance-testing-spec.md`、`docs/daily-updates/2026-08-17.md`；不涉及代码与配置变更。
- 验证结果：已执行全文关键词与 `git diff --check` 一致性检查；旧的 task_id 鉴权、24 小时幂等复用、先提交后记录 task_id 及 P0 自动停止表述已清除。另使用无上下文独立读者测试状态机/幂等/鉴权/P0-P1 边界，并根据反馈补齐 dispatching 取消墓碑与补偿取消、快速回调乱序、调度/启动截止时间、回调 410 语义、幂等请求哈希、P0 诊断输出边界，同时删除无上传协议支撑的 summary 压缩/分片承诺。纯文档变更，未运行代码构建/测试。

## 复审性能测试 SPEC v2.4 并补全细节（v2.5）

- 变更内容：@oracle 复审 v2.4 结论「基本可进入 P0，上一轮中高优先级问题已全部收敛」，补 4 项实现细节 + 若干小项，修订至 v2.5：① dispatching 重试粘性绑定同一 executor_id + task_id（确认未接收才清空回退 queued）；② stopping 补 execution_failed 兜底转移与后端重启恢复动作；③ §4.3 执行器改动表补 submit 幂等、已取消 task_id 墓碑、routes.py 幂等返回码、callback_client 回调 Bearer 头 + 指数退避重试；④ 回调重试语义（收 410/401/403 停止）+ stopping 期间完成回调 409。另补：callback_token_hash 明确 SHA-256 无盐、queued 队列超时告警、幂等唯一索引冲突补偿路径、smoke 结果走 StartDebug taskID 轮询回传。
- 影响范围：`docs/performance-testing-spec.md`（修订至 v2.5）；不涉及代码变更。
- 验证结果：纯文档补全，待进入 P0 实施时再做构建/测试验证。

## 实施性能测试 P0 执行器侧

- 变更内容：按 `docs/performance-testing-spec.md` §4.3/§4.4/§5.1/§5.5/§7.1 实施性能测试 P0 执行器侧，底层压测引擎为 Grafana k6（独立二进制，运行时检测，不新增 Python 依赖）。① `models/task.py`：`TaskType` 新增 `perf`，`TaskCreate/TaskView` 新增 `callbackToken` 字段（回调 Bearer 凭据，与 task_id 分离）。② `core/config.py`：`supported_types` 加入 `perf`，新增 perf 并发槽位（`perf_max_concurrent=1`）、超时系数/缓冲、优雅停止宽限期、快照周期、取消墓碑 TTL、回调重试次数/退避基延迟等配置。③ 新增 `runners/perf_runner.py`：按 scenario_type + load_config 渲染 k6 脚本（baseline/soak→constant-vus，ramp/peak/stress→ramping-vus stages，mixed 返回错误），阈值结构化数组渲染为 `p(95)<500` 形式，固定 `summaryTrendStats` 与 `handleSummary` 写 report.json，headers/body JSON 转义防注入、`{{secret.x}}` 敏感引用转为 `__ENV.x` 注入；`shutil.which("k6")` 检测、`subprocess.Popen` 调 `k6 run --out json`、按 load_config 动态计算超时（总时长×系数+缓冲）、NDJSON 本地采样定期原子写快照兜底、report.json 解析 metrics + 阈值判定、取消优雅停止→宽限期→强杀并用快照生成部分结果。④ `task_manager.py`：注册 perf runner 并加分派分支，`submit` 对已存在 task_id 幂等返回现有视图、新增取消墓碑（带 TTL），`cancel` 对不存在任务记墓碑返回已取消，`_finish` 取消后保留部分结果。⑤ `callback_client.py`：回调带 `Authorization: Bearer <callbackToken>`，终态前指数退避重试，收 410/401/403 停止重试。⑥ `heartbeat_client.py`：`_checks` 加 `shutil.which("k6")` 探测与版本号，k6 可用时才上报 supportedTypes 含 perf。⑦ `routes.py`：创建/取消幂等命中返回 200。
- 影响范围：`executor/app/models/task.py`、`executor/app/core/config.py`、`executor/app/runners/perf_runner.py`（新增）、`executor/app/services/task_manager.py`、`executor/app/services/callback_client.py`、`executor/app/services/heartbeat_client.py`、`executor/app/api/routes.py`、`executor/tests/test_perf_runner.py`（新增）。
- 验证结果：`python -m pytest tests/ -q` 为 181 passed、2 skipped，无失败；新增 `tests/test_perf_runner.py` 21 项全部通过（脚本生成各场景 options/阈值渲染、超时动态计算、k6 缺失检测、submit 幂等、取消墓碑、callback 重试停止）。真实 k6 联调依赖环境安装 k6 二进制，未在本机验证。

## 实施性能测试 P0 后端

- 变更内容：按 `docs/performance-testing-spec.md`（v2.5）实施性能测试 P0 后端，从占位升级为真实执行闭环。① 数据模型：`perf_test_plans` 增加 scenario_type/load_config/environment，阈值改为平台结构化数组（metric/aggregation/operator/value/unit/abortOnFail/delayAbortEval）；`perf_test_runs` 补齐 20+ 列（plan_snapshot/config_hash/executor_id/task_id/callback_token_hash/idempotency_key/三个 deadline/script_hash/generator_version/failure_stage/diagnostic_output/needs_attention/series 等）与三个唯一索引（task_id、triggered_by+idempotency_key、同方案活动任务部分唯一索引）；旧列 load_mode/vus/duration/stages 本版本保留并幂等回填，注释下一版本删除。② 状态机：pending→queued→dispatching→dispatched→running→stopping + 5 终态，全走条件更新（`where status = ...`），非法转移 409；dispatching 重试粘性绑定同一 task_id/executor_id。③ 幂等：RunPlan 带 idempotency_key，同 key 同 config 返原 run、不同 config 409，唯一索引冲突后重查判定。④ 调度：独立调度器（1s 调度 + 10s 恢复双 ticker），pickExecutor 校验在线 + supportedTypes 含 perf + k6 检测，先持久化 task_id/凭据哈希/executor_id 再幂等下发，回调 Bearer Token 与 task_id 分离、终态同事务撤销、410/409 语义。⑤ 恢复：扫描 dispatching/dispatched/running/stopping，只查询只补偿取消、绝不自动重投，无法确认置 needs_attention。⑥ expected_finish_at 按 load_config 动态计算（支持 Go 复合时长 + 场景求和）。⑦ smoke 接口（POST /perf/smoke + GET /perf/smoke/:taskId）。⑧ 权限三档 perf.plan.read/manage/execute；完成/失败通知。⑨ 修复下发 payload 字段契约：dispatchRun/StartSmoke 改为 snake_case 展开（scenario_type/load_config/target/method/headers/body/thresholds），对齐执行器 perf_runner 读取字段。
- 影响范围：`backend/internal/model/performance.go`、`backend/internal/repository/performance_repository.go`、`backend/internal/service/performance_service.go`、`backend/internal/controller/performance_controller.go`、`backend/internal/router/router.go`、`backend/cmd/api/main.go`、`backend/cmd/api/bootstrap.go`、`backend/cmd/api/api_migration.go` 及对应测试文件。
- 验证结果：`go build ./...`、`go vet ./...`、`go test ./...` 全部通过（新增/更新 service、controller 测试覆盖状态机条件更新、幂等、回调鉴权、expected_finish_at、smoke、payload 展开）。

## 修复性能测试 run 停留 pending

- 变更内容：`RunPlan` 创建或幂中命中 `pending` run 后，通过条件更新明确推进 `pending→queued`，并处理并发状态已变化的情况；调度器同时扫描遗留 `pending` 记录，推进成功后再分发，避免 run 永久停留在调度器未扫描的状态。新增 RunPlan 排队回归测试并调整相关 fake 仓储。
- 影响范围：`backend/internal/service/performance_service.go`、`backend/internal/service/performance_service_test.go`、`backend/internal/controller/performance_controller_test.go`。
- 验证结果：`go build ./...`、`go vet ./...`、`go test ./...` 通过；真实 API run #5 已验证 `queued→dispatched→execution_failed`，未再停留 `pending`。终态失败原因为本机执行器环境未找到 k6（`failure_stage=startup`、`error_message=未安装 k6，无法执行性能测试`）。

## 实施性能测试 P0 前端

- 变更内容：按 SPEC §8/§9 实施性能测试 P0 前端。① 信息架构：性能测试重构为独立路由页面（/performance/plans、/performance/plans/:id/edit、/performance/plans/:id、/performance/runs、/performance/runs/:id），刷新恢复页面。② 方案编辑页四区块：基本信息（场景类型用场景卡片而非下拉）、请求配置（Params/Headers/Body 分 Tab、Body JSON 格式化校验、敏感 Header 变量引用 {{secret.api_token}}、冒烟测试请求按钮）、负载配置（按场景渲染控件 + 右侧实时负载预览：总预计时长/最大并发/预计请求量/时间轴/安全限制）、阈值设置（结构化表格 + 宽松/常规SLA/严格/自定义模板 + 查看生成 k6 配置）。③ 执行确认面板（目标域名/环境/场景/最大并发/预计时长/阈值/执行器/是否生产/同方案活动任务，生产二次确认输入方案名称），触发带幂等 key，成功后跳转执行详情；P0 取消批量执行。④ 报告详情三屏：结论（通过/性能未达标/执行异常，图标+文字）、阈值结果（逐条表达式/实际值/目标值/差距/通过失败）、诊断信息（执行器与 k6 版本/退出码/失败阶段/错误信息/脱敏日志/原始 summary 折叠/方案快照），区分五种终态失败类型，指标空值显示「--」。⑤ 权限：perf.plan.manage/execute 控制编辑、执行/取消按钮显隐。
- 影响范围：`frontend/src/pages/PerfPlansPage.js`、`PerfRunsPage.js`、`PerformancePage.js`（改），`PerfPlanEditPage.js`、`PerfPlanDetailPage.js`、`PerfRunDetailPage.js`、`components/PerfRunConfirmPanel.js`（新增），`services/performanceService.js`、`utils/perf.js`、`utils/routeState.js`、`styles/global.css` 及测试。
- 验证结果：`npm run build` 通过（仅既有 chunk size 提示）；`npm run test` 新增组件/工具测试通过。

## 完成性能测试 P0 真实 k6 闭环联调

- 变更内容：安装 Grafana k6 v2.2.0（`C:\Program Files\k6\k6.exe`，系统 PATH 已注册），用当前源码执行器替换旧打包版进程进行联调。真实链路依次暴露并修复 4 个运行时缺陷：① `perf_test_runs.error_message` 可为 NULL，但 repository 直接扫描到 string，导致 GetRun/ListRuns 报「读取执行记录失败」，改用 `sql.NullString` 并补完整 39 列扫描测试；② RunPlan 创建后缺 `pending→queued` 推进，调度器只扫描 queued，导致永久 pending，现由触发路径条件推进且调度器兜底历史 pending；③ 后端 perf payload 使用 camelCase 且把配置塞入 snapshot，而执行器读取 snake_case 展开字段，现统一为 `scenario_type/load_config/target/method/headers/body/thresholds`，smoke 的 `targetUrl` 同步改为 `target`；④ 执行器仍按通用 TaskView（success/failed + nested result）回调，而后端要求扁平 PerfCallbackRequest，现 perf 使用专用 running/终态回调，扁平回填指标与诊断字段，回调 Bearer Token 与重试策略保持不变。此前 run #5 记录的「未安装 k6」为启动确认超时后的表象，后续 direct runner 已证明同进程 k6 可正常执行，真实根因为回调契约不一致。
- 影响范围：`backend/internal/repository/performance_repository.go`、`backend/internal/repository/performance_repository_test.go`、`backend/internal/service/performance_service.go`、`backend/internal/service/performance_service_test.go`、`backend/internal/model/performance.go`；`executor/app/services/callback_client.py`、`executor/app/services/task_manager.py`、`executor/app/runners/perf_runner.py` 及相关测试；运行环境安装 k6 v2.2.0，并以源码执行器监听 `127.0.0.1:8091`。
- 验证结果：执行器心跳为 online，`supportedTypes` 含 perf、`checks.k6=true`；直接 runner 1 VU/3s 成功（3571 请求、P95=3ms）；平台完整 run #7 状态轨迹 `queued→dispatching→running→completed`，最终 totalRequests=14525、P95=1ms、errorRate=0、RPS≈2904.84、exitCode=0、durationMs=6445、k6Version=`k6.EXE v2.2.0`，两次 perf 回调均 HTTP 200。后端 `go build ./...`/`go vet ./...`/`go test ./...` 通过，执行器 `pytest` 为 185 passed、2 skipped，前端 `npm run build` 通过。

## 完成性能测试 P1 实时监控与混合场景

- 变更内容：完成 mixed 多接口按权重随机选流、`abortOnFail`/`delayAbortEval`、1～5 秒窗口采样、执行器事件回调、后端内存 Hub 与断点续传 SSE、最多 3000 点的全程确定性降采样和终态 `series` 持久化；运行详情新增 RPS、P50/P90/P95/P99、错误率、VU 趋势图，以及状态码、错误 Top N、实时阈值、消息和断线恢复。同步修复 smoke 误跑 1 分钟、mixed Base URL、500 VU 硬上限、stress 50 阶段时长、启动前失败终态、回调 Token 泄露、queued 取消竞态、高吞吐窗口低报和终态回调超 4 MiB 等问题；清理前端过时的 P0 占位提示。
- 影响范围：性能测试后端 controller/model/repository/service/router 与新增事件 Hub；执行器 perf runner、task model/manager、callback client 和配置；前端方案编辑、执行确认、运行详情、SSE service、性能工具与样式；对应三端测试。
- 验证结果：后端 `go build ./...`、`go vet ./...`、`go test ./...` 全部通过；执行器 `python -m pytest tests/ -q` 为 `208 passed, 2 skipped`；前端 `npm run test` 为 123 项通过，`npm run build` 通过（仅既有 chunk size 提示），`git diff --check` 通过。真实 mixed run #27（绝对 URL、无 Base URL）和 #28（相对 URL）均完成；abort run #24 为 `threshold_failed/partial=true`，普通阈值 run #25 为 `threshold_failed/partial=false`；run #30 收到 sample/terminal SSE，sample 包含状态码、错误 Top N、阈值与消息；真实 smoke 任务仅执行 1 次请求。终态大 payload 测试确认 body ≤4 MiB，超过 3000 点时保留首尾及全程 stage 边界代表。
